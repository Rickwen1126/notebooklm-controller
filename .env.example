# NotebookLM Controller — Environment Variables
# Copy to .env and customize as needed.

# Debug mode (verbose logging)
# NBCTL_DEBUG=1

# Override locale (default: auto-detect from Chrome navigator.language)
# Also configurable via ~/.nbctl/config.json { "locale": "zh-TW" }
# NBCTL_LOCALE=zh-TW
