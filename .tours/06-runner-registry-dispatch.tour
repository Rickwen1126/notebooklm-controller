{
  "$schema": "https://aka.ms/codetour-schema",
  "title": "06 - Module Overview: Runner Registry Dispatch — Formal Scheduler Chain for Batch Operations",
  "steps": [
    {
      "title": "Context",
      "description": "## 白話導讀\n\n這條 tour 在看：**register_all_notebooks 如何從「MCP tool 裡面 inline 做所有事」重構成「走正式的 Scheduler → Dispatcher → Runner 調度鏈」**。\n\n如果先記不住細節，先抓住一個 mental model：**Runner Registry 就像 HTTP router 的 handler map**。Dispatcher 是 middleware pipeline（URL 解析、tab 取得、viewport 設定、operation log），Runner 是 route handler（pipeline 跑 Planner+Script+Recovery，scanAllNotebooks 跑批次掃描）。MCP tool 只負責「組 request + submit」，就像 controller 只負責 parse params + 呼叫 service。\n\n## 在解決什麼問題\n\n之前的 register_all_notebooks 直接在 MCP tool handler 裡 acquireTab、setViewport、buildScriptContext、跑 loop。這繞過了 Scheduler 的 task lifecycle（queued → running → completed），所以：\n1. task 沒有 JSON 持久化 — daemon crash 時沒有紀錄\n2. tab pool 管理自己來 — 可能和 Scheduler 衝突（Scheduler 以為 tab 都空閒）\n3. operation log 不會記錄 — 這條操作在歷史裡消失\n4. 沒有 degraded mode 保護 — Scheduler 判定 agent runtime 異常時仍可提交\n5. 腳本失敗沒有 Recovery — script-first 的「LLM on failure」機制完全缺席\n\n重構後，register_all_notebooks 變成一個 thin submitter，只做 `scheduler.submit({ runner: 'scanAllNotebooks' })`。所有 shared concerns 由 Dispatcher 處理，業務邏輯由 Runner 處理。\n\n## 為什麼值得讀\n\n1. 看到 `AsyncTask.runner` 欄位如何讓 task model 從「只有 pipeline 一條路」擴展成「多 runner 分派」\n2. 理解 Dispatcher 如何把 URL 解析、tab 取得、viewport、operation log 變成 shared concerns，任何 runner 都不用重複寫\n3. 學到 per-notebook recovery 的 script → recovery → goBack loop 模式\n4. 看到 RUNNER_REGISTRY 的 open-closed 設計 — 加新 runner 不改 Dispatcher"
    },
    {
      "file": "src/shared/types.ts",
      "line": 129,
      "ref": "6a34cdfc5b2587e86ac0e600f5ce8e165143136b",
      "title": "1. AsyncTask model — runner + runnerInput 欄位",
      "description": "## Insight\n\n**機制**：`runner` 是 string（不是 enum），用 registry lookup 做 dispatch。`runnerInput` 是 `Record<string, unknown> | null`，讓每個 runner 可以有自己的 input schema（scanAllNotebooks 目前不需要額外 input，所以是 null）。\n\n**故障模式**：`runner` 用 string 而非 union type，表示 typo 不會在 compile time 被抓到。如果 MCP tool submit 時打錯（`scanAllNotebook` 少了 s），只會在 runtime dispatch 時收到 `Unknown runner: scanAllNotebook` 錯誤。這是有意的 — 讓 registry 可以動態擴展（例如 plugin 注入 runner），但犧牲 type safety。\n\n**設計取捨**：選擇在 AsyncTask 永久化 `runner` 名稱，而非只在調度時使用。好處是 task JSON 自帶「誰跑了我」的 metadata，debug 和 audit 時不需要去 log 裡對照。壞處是 runner rename 需要考慮 migration（舊 task JSON 裡存的是舊名稱）。\n\n## 白話解析\n\n這兩個欄位是整個 runner registry 的根基。之前 AsyncTask 只有 command（自然語言指令），Dispatcher 永遠交給 pipeline。現在加了 runner（交給誰跑）和 runnerInput（跑的時候要什麼額外參數），task 變成「帶路由資訊的工單」。\n\n## Code Detail\n\n**這段在幹嘛**：在 AsyncTask interface 加入 `runner: string` 和 `runnerInput: Record<string, unknown> | null`。\n\n**為什麼這樣寫**：\n- 好處：runner 名稱持久化在 task JSON 裡 — crash 後可以知道哪個 runner 在處理\n- 好處：runnerInput 用 Record<string, unknown> 而非 generic type — 避免 AsyncTask<T> 泛型感染所有 caller\n- 壞處：string runner 沒有 compile-time validation，typo 只在 runtime 爆\n\n**思考問題**：\n1. 如果改成 `runner: keyof typeof RUNNER_REGISTRY`，會有什麼問題？（提示：types.ts 不應依賴 daemon/index.ts）\n2. `runnerInput` 為什麼是 `null` 而非 `undefined`？（提示：JSON.stringify 的差異）"
    },
    {
      "file": "src/state/task-store.ts",
      "line": 73,
      "ref": "6a34cdfc5b2587e86ac0e600f5ce8e165143136b",
      "title": "2. TaskStore.create() — runner defaults to 'pipeline'",
      "description": "## Insight\n\n**機制**：`params.runner ?? \"pipeline\"` 讓所有舊的 `taskStore.create()` 呼叫（不帶 runner 參數的）自動走 pipeline。這是 backwards compatibility 的關鍵 — Scheduler 本來就透過 `taskStore.create()` 建 task，只是以前沒有 runner 概念。現在加了 runner，但預設值保證舊路徑不壞。\n\n**故障模式**：如果預設值是 `undefined`（不設預設），Dispatcher 裡的 `RUNNER_REGISTRY[task.runner]` 會 lookup `undefined` key，回傳 `undefined`，fail fast 回傳 `Unknown runner: undefined`。所有不帶 runner 的 exec task 全部失敗。\n\n**設計取捨**：把 \"pipeline\" 這個字串常數分散在 TaskStore 和 Dispatcher 兩處（Dispatcher 也有 `task.runner ?? \"pipeline\"`）。好處是 defense in depth — 任何路徑都能 fallback。壞處是 magic string 散落，如果要 rename 需要改兩處。可以用 const 但目前選擇不做，因為只有兩處。\n\n## 白話解析\n\n這段是「新欄位不影響舊功能」的保險機制。之前所有 task 都走 pipeline，現在加了 runner 路由，但 90% 的 task（exec 呼叫的）仍然是 pipeline。`?? \"pipeline\"` 保證不帶 runner 的 create 呼叫繼續正常工作。\n\n## Code Detail\n\n**這段在幹嘛**：create 方法接受 optional `runner` 和 `runnerInput`，分別 fallback 到 `\"pipeline\"` 和 `null`。\n\n**為什麼這樣寫**：\n- 好處：backwards compatible — 舊的 exec path 不需改任何 code\n- 好處：task JSON 永遠有 runner 值（不是 undefined），JSON 結構一致\n- 壞處：\"pipeline\" 這個 magic string 沒有集中定義\n\n**思考問題**：\n1. `runnerInput ?? null` 而非 `runnerInput ?? {}` — 為什麼用 null？（提示：語義上 null = 不適用，{} = 適用但無參數）\n2. 如果 TaskStore 是 shared library（被其他 project 引用），\"pipeline\" default 是好設計嗎？"
    },
    {
      "file": "src/daemon/scheduler.ts",
      "line": 80,
      "ref": "6a34cdfc5b2587e86ac0e600f5ce8e165143136b",
      "title": "3. Scheduler.submit() — passes runner through",
      "description": "## Insight\n\n**機制**：Scheduler.submit() 是 MCP tool 和 TaskStore 之間的中間層。它做三件事：(1) 檢查 shuttingDown / degraded 狀態（gate），(2) 呼叫 taskStore.create()（persist），(3) 排入 execution queue（dispatch）。runner 和 runnerInput 被透傳（pass-through），Scheduler 不關心具體是什麼 runner。\n\n**故障模式**：如果 Scheduler 處於 degraded 狀態（連續 timeout 超過閾值），submit 直接 throw。這保護了 scanAllNotebooks 這種長時間操作 — 如果 agent runtime 已經不穩定，不應該再提交 90 個 notebook 的批次掃描。\n\n**設計取捨**：Scheduler 不做 runner validation（不檢查 runner name 是否在 RUNNER_REGISTRY 裡）。Validation 延遲到 Dispatcher 的 runtime lookup。好處是 Scheduler 和 RUNNER_REGISTRY 解耦（Scheduler 甚至不 import RUNNER_REGISTRY），壞處是 invalid runner 的 task 會被建立並 persist 在 disk 上，直到 Dispatcher 執行時才 fail。\n\n## 白話解析\n\n這段是「MCP tool 提交任務」的通用入口。不管你是 exec（走 pipeline）還是 register_all_notebooks（走 scanAllNotebooks），都從這裡進來。Scheduler 只負責 gate + persist + queue，不管你要走哪條路。\n\n## Code Detail\n\n**這段在幹嘛**：接受 runner/runnerInput 參數，透傳給 taskStore.create()，然後排入 queue。\n\n**為什麼這樣寫**：\n- 好處：Scheduler 是 runner-agnostic — 加新 runner 不需改 Scheduler\n- 好處：degraded mode 統一保護所有 runner（不是只保護 pipeline）\n- 壞處：invalid runner 的 task 會浪費一次 task create + disk I/O，直到 Dispatcher 才 fail\n\n**思考問題**：\n1. Scheduler 為什麼不在 submit 時就 validate runner name？（提示：dependency direction — Scheduler 不應該知道有哪些 runner）\n2. 如果 `runner: \"scanAllNotebooks\"` 的 task 已經在跑，再 submit 一個會怎樣？（提示：tab pool capacity）"
    },
    {
      "file": "src/daemon/index.ts",
      "line": 100,
      "ref": "6a34cdfc5b2587e86ac0e600f5ce8e165143136b",
      "title": "4. RUNNER_REGISTRY + TaskRunner type — the dispatch map",
      "description": "## Insight\n\n**機制**：`TaskRunner` 是一個 function type — `(task, tabHandle, deps) => Promise<SchedulerSessionResult>`。每個 runner 拿到的 tabHandle 已經由 Dispatcher 取得（tab pool）並設好 viewport。`RUNNER_REGISTRY` 是 `Record<string, TaskRunner>`，用 runner name 做 key。\n\n**故障模式**：如果有人在 RUNNER_REGISTRY 裡註冊了一個 runner 但 signature 不符合 TaskRunner type，TypeScript compile 會擋住。但如果用 runtime dynamic registration（`RUNNER_REGISTRY[name] = fn`），TypeScript 不會驗 fn 的 return type 裡 `result` 是否是 object — `SchedulerSessionResult.result` 是 `object | undefined`，但 runner 可能回傳 `{ success: true, result: \"string\" }`（string 不是 object），compile 不報錯但 downstream cast 可能出問題。\n\n**設計取捨**：Registry 是 module-level const（不是 class、不是 factory）。好處是 plain object，import 即用。壞處是不支援 lifecycle hooks（beforeRegister、afterRegister），如果未來需要 runner validation 或 metrics，要改成 class-based registry。\n\n## 白話解析\n\n這段定義了「跑什麼用什麼函式」的對照表。目前有兩個 runner：`pipeline`（預設的 Planner → Script → Recovery 流程）和 `scanAllNotebooks`（批次掃描）。加新 runner 就是在這個 object 裡加一行。\n\n## Code Detail\n\n**這段在幹嘛**：定義 `TaskRunner` type，export `RUNNER_REGISTRY` 並註冊兩個 runner。\n\n**為什麼這樣寫**：\n- 好處：open-closed — 加新 runner 只需加 import + 加一行 registry entry\n- 好處：type-safe — TaskRunner signature 強制所有 runner 拿同樣的 (task, tabHandle, deps)，回傳同樣的 SchedulerSessionResult\n- 壞處：plain object 不支援 duplicate key detection（但 TS 的 Record literal 會 compile error on duplicate key）\n\n**思考問題**：\n1. 為什麼 RUNNER_REGISTRY 是 export 的？誰需要從外部讀它？（提示：test verification）\n2. 如果 runner 需要不同的 deps（例如 scanAllNotebooks 需要 stateManager 但 pipeline 不需要），目前的 RunTaskDeps 是怎麼處理的？（提示：所有 runner 都拿到完整 deps，用不到的就不取）"
    },
    {
      "file": "src/daemon/index.ts",
      "line": 175,
      "ref": "6a34cdfc5b2587e86ac0e600f5ce8e165143136b",
      "title": "5. Dispatcher (createRunTask) — shared concerns + runner dispatch",
      "description": "## Insight\n\n**機制**：`createRunTask` 是 factory function，回傳 `(task) => Promise<SchedulerSessionResult>`。這個回傳的函式就是 Scheduler 的 `runTask` callback。它處理五個 shared concerns，然後 dispatch 到 runner：\n1. **Runner lookup** — fail fast if unknown (line 186)\n2. **URL resolution** — stateManager.getNotebook() 或 NOTEBOOKLM_HOMEPAGE (line 196)\n3. **Tab acquisition** — tabManager.acquireTab() from pool (line 210)\n4. **Viewport contract** — Emulation.setDeviceMetricsOverride 1920x1080 (line 224)\n5. **Operation log** — cacheManager.addOperation() after runner completes (line 234)\n\n然後一行 `runner(task, tabHandle, deps)` 完成 dispatch (line 229)。\n\n**故障模式**：如果 runner 內部 throw（非 graceful return），exception 會被 finally block 的 tab release 吃掉嗎？不會 — exception propagation 正常，finally 只做 releaseTab 和 temp file cleanup，不 catch。但 operation log 記錄在 `try` block 裡的 runner 之後 — 如果 runner throw，operation log 不會被記錄。這是有意的：throw 代表 infrastructure failure（不是 business failure），不應該記在 operation log 裡。\n\n**設計取捨**：所有 shared concerns 在 Dispatcher 裡，而非在每個 runner 裡。之前的 register_all_notebooks inline 實作自己做 acquireTab + setViewport，pipeline 也自己做。重構後統一在 Dispatcher，runner 只需要專注業務邏輯。代價是 runner 不能 customize 這些 shared concerns（例如不能選擇不設 viewport），但目前所有 runner 都需要這些。\n\n## 白話解析\n\n這段是整個 dispatch 鏈的核心。你可以把它想成 Express middleware stack — URL 解析、tab 取得、viewport 設定都是 middleware，runner 是最終的 route handler。任何新 runner 都自動享有這些 middleware 的保護，不需要自己處理。\n\n## Code Detail\n\n**這段在幹嘛**：factory 回傳 dispatch function，處理 shared concerns + dispatch + cleanup。\n\n**為什麼這樣寫**：\n- 好處：DRY — URL 解析 / tab 管理 / viewport / logging 只寫一次\n- 好處：fail-fast — unknown runner 在做任何 I/O 之前就 return error\n- 好處：finally block 保證 tab release — runner crash 不會 leak tab\n- 壞處：runner throw 時 operation log 不記錄 — 需要到 structured log 才能查到\n\n**思考問題**：\n1. `__homepage__` alias 走 hardcoded `NOTEBOOKLM_HOMEPAGE` URL — 這個 special case 是只有 scanAllNotebooks 需要嗎？（提示：deleteNotebook 也在 homepage 操作）\n2. 為什麼 viewport 設定在 Dispatcher 而非 runner 裡？如果未來有 runner 需要不同解析度怎麼辦？"
    },
    {
      "file": "src/daemon/index.ts",
      "line": 110,
      "ref": "6a34cdfc5b2587e86ac0e600f5ce8e165143136b",
      "title": "6. runPipelineTask — the default runner (G2 script-first)",
      "description": "## Insight\n\n**機制**：`runPipelineTask` 是從原本 createRunTask 裡面抽出來的 — 之前 Dispatcher 直接包含 pipeline 邏輯，現在拆成獨立函式。它做三件事：(1) 確保 tab 在正確 URL 上（tab pool reuse 可能殘留在別的頁面），(2) buildToolsForTab（Recovery session 需要），(3) runPipeline（Planner → Script → Recovery）。\n\n**故障模式**：Navigation check `!currentUrl.startsWith(targetUrl)` 是 prefix match — 如果 targetUrl 是 `https://notebooklm.google.com/notebook/abc` 而 currentUrl 是 `https://notebooklm.google.com/notebook/abcdef`，prefix match 誤判為已在正確頁面。實務上 NotebookLM URL 的 notebook ID 是 hash-based（不會有 prefix 衝突），但這是一個 subtle 的假設。\n\n**設計取捨**：抽成函式而非 class。好處是保持 functional style，和其他 runner（runScanAllNotebooksTask）風格一致。壞處是沒有 lifecycle hooks（beforeRun、afterRun），如果未來 pipeline runner 需要特殊的 pre/post 邏輯，要加在函式內部而非 hook。\n\n## 白話解析\n\n這段是「原本 Dispatcher 在做的事」被搬出來變成一個 runner。之前 Dispatcher 就是 pipeline runner（只有一條路），現在 Dispatcher 變成 middleware，pipeline 邏輯搬到 runPipelineTask。\n\n## Code Detail\n\n**這段在幹嘛**：確保 tab 在正確 URL → 建立 tools → 呼叫 runPipeline → 回傳 SchedulerSessionResult。\n\n**為什麼這樣寫**：\n- 好處：pipeline runner 和 Dispatcher 解耦 — Dispatcher 不知道 pipeline 的內部邏輯\n- 好處：tools 只在 pipeline runner 裡 build — scanAllNotebooks runner 不需要 Copilot SDK tools\n- 壞處：`await new Promise(r => setTimeout(r, 2000))` 是 hardcoded sleep — navigation 後等 2 秒，不是 polling\n\n**思考問題**：\n1. 為什麼 `isHomepage` case 不需要 navigate？（提示：homepage task 通常已經在 homepage）\n2. `buildToolsForTab` 在這裡 build 但只有 Recovery session 使用 — Planner session 不用 tools。為什麼不延遲到 recovery 時才 build？（提示：runPipeline 的 interface 要求 tools 參數）"
    },
    {
      "file": "src/agent/scan-notebooks-runner.ts",
      "line": 73,
      "ref": "6a34cdfc5b2587e86ac0e600f5ce8e165143136b",
      "title": "7. scanAllNotebooks runner — entry point",
      "description": "## Insight\n\n**機制**：`runScanAllNotebooksTask` 符合 `TaskRunner` signature — `(task, tabHandle, deps) => Promise<SchedulerSessionResult>`。它拿到的 tabHandle 已經指向 `NOTEBOOKLM_HOMEPAGE`（因為 MCP tool submit 時 notebookAlias 是 `__homepage__`），viewport 已經設好 1920x1080。Runner 只需要專注：buildScriptContext → extractNames → per-notebook loop。\n\n**故障模式**：`_task` 前綴的 underscore 表示 task 的 command/context 沒有被使用 — scanAllNotebooks 不需要自然語言指令，它的行為是固定的。但 task.taskId 其實在 error reporting 時可能有用（目前沒用到），如果未來需要在 repair log 裡記 taskId，需要把 `_task` 改回 `task`。\n\n**設計取捨**：Runner 回傳 `{ success, result?, error? }` 而非完整的 `ScanAllNotebooksResult`。`ScanAllNotebooksResult` 被包在 `result` 欄位裡。這是因為 `SchedulerSessionResult` 是通用 interface — 所有 runner 都回傳同樣的 shape，runner-specific 的 data 藏在 `result: object` 裡。好處是 Dispatcher 不需要知道每個 runner 的具體 result type。壞處是消費端需要 type assertion（`result as ScanAllNotebooksResult`）。\n\n## 白話解析\n\n這段是 scanAllNotebooks runner 的入口。它和 pipeline runner 的差異：pipeline 走 Planner → Script → Recovery（一個 task 執行一個 command），scanAllNotebooks 走 extractNames → per-notebook loop（一個 task 處理所有 notebook）。共同點是都拿到 Dispatcher 準備好的 tabHandle 和 deps。\n\n## Code Detail\n\n**這段在幹嘛**：初始化 result accumulators（registered, skipped, recovered, errorReport），buildScriptContext，extractNotebookNames。\n\n**為什麼這樣寫**：\n- 好處：runner 不管 tab lifecycle（Dispatcher 處理）— 簡化 runner 邏輯\n- 好處：ScanAllNotebooksResult interface 完整定義了成功/跳過/恢復/失敗四個分類 — 消費端一目了然\n- 壞處：`_task` unused — 如果 taskId propagation 需求出現，容易忘記改回 `task`\n\n**思考問題**：\n1. buildScriptContext 需要 cdpSession + page + uiMap — 這三個分別從哪來？（提示：tabHandle 有前兩個，deps 有第三個）\n2. 如果 extractNotebookNames 失敗（homepage 沒載入），runner 直接 return error — 為什麼不走 Recovery？（提示：extractNotebookNames 是 runner 的前置條件，不是 per-notebook 的操作）"
    },
    {
      "file": "src/agent/scan-notebooks-runner.ts",
      "line": 126,
      "ref": "6a34cdfc5b2587e86ac0e600f5ce8e165143136b",
      "title": "8. Per-notebook loop — script → recovery → goBack",
      "description": "## Insight\n\n**機制**：每個 notebook 經歷四個階段：\n1. `scriptedGetNotebookUrl(ctx, name)` — 確定性腳本：scrollIntoView → click → waitForNavigation → read URL → goBack\n2. 成功 → URL dedup check → `stateManager.addNotebook()` → continue\n3. 失敗 → `runRecoverySession({ goal, scriptResult })` — LLM 看到腳本失敗的 step/selector，用 browser tools 修復\n4. Recovery 成功/失敗 → `saveRepairLog()` → goBack to homepage → continue\n\n這就是 G2 script-first 的完整體現：90 個 notebook 裡如果 85 個腳本成功，只有 5 個需要 LLM Recovery。\n\n**故障模式**：Recovery 成功後用 `page.url()` 取 URL（browser state authority）而非 Recovery session 回傳的文字 — 因為 LLM 可能 hallucinate URL，但 `page.url()` 是 browser 的 ground truth。Recovery 後的 `goBack()` 可能失敗（如果 Recovery 做了多次 navigation），用 try-catch best-effort 處理，不阻塞整個 loop。\n\n**設計取捨**：per-notebook 獨立 recovery 而非整批 retry。好處是一個 notebook 失敗不影響其他 89 個。壞處是 Recovery session 需要每次重新建立（沒有 session reuse），每次 Recovery ~8-12 秒。如果 20 個 notebook 需要 Recovery，整個操作多花 ~3 分鐘。\n\n## 白話解析\n\n這段是批次掃描的核心 loop。你可以把它想成 for 迴圈裡的 try-catch：腳本能跑就跑（0 LLM cost），跑不過就叫 LLM 來救（Recovery cost），LLM 也救不了就記下來繼續下一個（不 abort 整個操作）。每個 notebook 獨立成敗，最後彙整成 report。\n\n## Code Detail\n\n**這段在幹嘛**：per-notebook 的 script → check → register → recovery → register → goBack flow。\n\n**為什麼這樣寫**：\n- 好處：per-notebook isolation — 一個失敗不 abort 全部\n- 好處：browser state authority — Recovery 成功後用 `page.url()` 取 URL，不信 LLM 回傳的文字\n- 好處：repair log 持久化 — 即使 Recovery 也失敗，仍然記錄 script failure + recovery attempt，供 offline 分析\n- 壞處：goBack best-effort — 如果 goBack 失敗，下一個 notebook 的 scriptedGetNotebookUrl 可能不在 homepage，導致 extractNames 之後的所有 notebook 都失敗\n\n**思考問題**：\n1. Recovery goal prompt 是 `Navigate to notebook \"${name}\" and obtain its URL` — 這夠 explicit 嗎？（提示：GPT-4.1 需要 step-by-step recipe，但 Recovery 用的是 gpt-5-mini）\n2. 如果 notebook 名稱含有雙引號，goal prompt 會壞嗎？（提示：template literal 裡的引號）\n3. `saveRepairLog` 在 Recovery 成功和失敗時都被呼叫 — 成功的 repair log 有什麼用？（提示：UIMap 學習機制）"
    },
    {
      "file": "src/daemon/notebook-tools.ts",
      "line": 389,
      "ref": "6a34cdfc5b2587e86ac0e600f5ce8e165143136b",
      "title": "9. Thin submitter — collapsed MCP tool",
      "description": "## Insight\n\n**機制**：整個 MCP tool handler 從之前的 ~120 行（acquireTab + setViewport + buildScriptContext + extractNames + loop + releaseTab）壓縮成 ~25 行。它只做四件事：(1) check deps, (2) scheduler.submit with runner name, (3) waitForTask, (4) read result。所有業務邏輯和 infrastructure concerns 都交出去了。\n\n**故障模式**：`waitForTask` 是 blocking wait — MCP tool handler 在這裡等 scanAllNotebooks runner 完成。如果 90 個 notebook 花 5 分鐘，MCP client 需要能處理 5 分鐘的 long-running request。這和 exec 不同 — exec 回傳 taskId 讓 client 自己 poll。register_all_notebooks 選擇 sync wait 是因為 client（Claude Code）通常期望 tool 回傳最終結果，不想做 poll loop。\n\n**設計取捨**：用 `scheduler.submit({ runner: \"scanAllNotebooks\" })` 指定 runner，而非 `scheduler.submitScanAll()` 專用方法。好處是 Scheduler API 不膨脹（加新 runner 不加新 method）。壞處是 \"scanAllNotebooks\" magic string — 如果 runner rename 需要改這裡。\n\n## 白話解析\n\n這段是重構的「before/after」最明顯的地方。之前這裡有 acquireTab、setViewport、dynamic import、for loop、stateManager.addNotebook... 現在就五行：submit → wait → read result。MCP tool 變成 thin controller，所有重活都在 runner 裡。\n\n## Code Detail\n\n**這段在幹嘛**：MCP tool handler — submit task with runner name, wait, return result。\n\n**為什麼這樣寫**：\n- 好處：MCP tool 層不碰 browser / tab / viewport / script — 關注點分離到極致\n- 好處：走正式 Scheduler → task lifecycle 有 JSON persist、degraded mode 保護、operation log\n- 壞處：sync wait — 如果 daemon timeout 設得比 scan 時間短，tool handler 會被中斷\n- 壞處：magic string \"scanAllNotebooks\" — 沒有 const 保護\n\n**思考問題**：\n1. `notebookAlias: \"__homepage__\"` — 為什麼用 special alias 而非 null？（提示：AsyncTask.notebookAlias 是 required string）\n2. 如果使用者同時從兩個 Claude Code session 呼叫 register_all_notebooks，會發生什麼？（提示：Scheduler queue + tab pool capacity）"
    },
    {
      "file": "src/scripts/operations.ts",
      "line": 951,
      "ref": "6a34cdfc5b2587e86ac0e600f5ce8e165143136b",
      "title": "10. scriptedExtractNotebookNames — runner-internal script",
      "description": "## Insight\n\n**機制**：這個腳本不在 SCRIPT_REGISTRY 裡（不對外暴露給 Planner/Executor）。它是 scanAllNotebooks runner 的 internal implementation detail。只做一件事：從 homepage DOM 的 `.project-table-title` 元素萃取 notebook 名稱列表。\n\n**故障模式**：依賴 `.project-table-title` CSS class — 這是 Angular Material 的 component 內部 class。Google 可以隨時改。但這個 class 在 NotebookLM 上已經穩定超過 6 個月，且 `tr[tabindex]` 和 `.project-table-title` 是 homepage 最穩定的兩個 selector（其他如 `.notebook-row` 在 2025Q3 改過一次）。\n\n**設計取捨**：runner-internal script 不註冊到 SCRIPT_REGISTRY，意味著 Planner 不能自然語言觸發「列出所有 notebook 名稱」。這是故意的 — extractNotebookNames 是 scan runner 的 building block，不是 user-facing operation。如果未來需要 user-facing 的「列出 notebook」，應該是新的 SCRIPT_REGISTRY entry + 對應 MCP tool，不是直接暴露這個 internal script。\n\n## 白話解析\n\n這段是「讀首頁有哪些 notebook」的腳本。它用 `querySelectorAll('.project-table-title')` 一次拿到所有名稱，然後用 `title` attribute（不帶 emoji 的乾淨名字）回傳。整個操作只需要一次 `page.evaluate`，不需要 click 或 navigation。\n\n## Code Detail\n\n**這段在幹嘛**：ensureHomepage → waitForRowsStable → querySelectorAll .project-table-title → 回傳 JSON array。\n\n**為什麼這樣寫**：\n- 好處：一次 evaluate 拿所有名字（vs. 逐一 query）— 90 個 notebook 只做 1 次 DOM query\n- 好處：`title` attribute 優先，fallback `textContent` — 處理有 emoji 和沒 emoji 兩種情況\n- 好處：不在 SCRIPT_REGISTRY — internal detail 不汙染 user-facing API surface\n- 壞處：如果 NotebookLM 改成 virtual scroll（只 render 可見 rows），querySelectorAll 只拿到 ~15 個\n\n**思考問題**：\n1. 這個腳本不做 Recovery — 如果 ensureHomepage 失敗，scanner 直接 return error。為什麼不像 per-notebook loop 一樣有 Recovery？（提示：如果連 homepage 都到不了，Recovery 也沒有 context 可以修復）\n2. runner-internal script 不在 SCRIPT_REGISTRY 但在 operations.ts — 有沒有更好的 file 放置位置？（提示：module cohesion vs. file organization）"
    }
  ]
}
