{
  "$schema": "https://aka.ms/codetour-schema",
  "title": "01 - Sky Eye: notebooklm-controller Phase 2 Foundation",
  "steps": [
    {
      "title": "Context: NotebookLM Controller 是什麼",
      "description": "## NotebookLM Controller 是什麼\n\n一個 MCP Server daemon，讓 AI agent 透過 Chrome 操控 Google NotebookLM。\n\n### 錨定已知\n\n如果你用過 **Puppeteer + Chrome DevTools Protocol (CDP)**，這就是那個概念加上三層包裝：\n- 底層：CDP raw commands（click, screenshot, type）\n- 中層：GitHub Copilot SDK agent runtime（session, tools, hooks）\n- 上層：MCP Server（Streamable HTTP）對外暴露 tool 給 Claude/ChatGPT 等 AI\n\n類比：就像 LSP server 讓任何編輯器用同一個語言服務，MCP server 讓任何 AI client 用同一套 NotebookLM 操作能力。\n\n### 這條路徑展示什麼 mental model\n\n**四層洋蔥架構**：\n```\nMCP Client (Claude) → MCP Server (daemon) → Copilot SDK (agent) → Chrome CDP (browser)\n```\n\n每一層只認識相鄰層。MCP Server 不知道 CDP 怎麼點擊；Agent tools 不知道 MCP protocol 怎麼傳輸。\n\n### 為什麼值得讀\n\n走完這個 tour 你會理解：\n1. 四層洋蔥的邊界在哪裡、為什麼這樣切\n2. 資料怎麼流（request → task → session → CDP → response）\n3. 每個模組的職責和不該做的事（boundary violations）\n4. 目前 Phase 2 是骨架，Phase 3+ 才接上真正的業務邏輯\n\n### 現況\n\n- **Phase 2 Foundational 完成**：36 tasks, 227 tests, 15 test files\n- **8 個模組**全部有 skeleton：shared, state, tab-manager, network-gate, agent, agent/tools, daemon, notification\n- **尚未連接**：模組之間的 wiring 在 Phase 3 (daemon entry point) 才發生"
    },
    {
      "file": "src/shared/types.ts",
      "line": 1,
      "ref": "157bae12d93932e241a337e33763bd307847a9b2",
      "title": "Layer 0: Data Model — 17 interfaces 定義系統語言",
      "description": "## 機制\n\n所有模組共用的型別定義，是整個系統的「共同語言」。分三類：\n\n1. **持久化型別**（存 JSON 檔）：`DaemonState`, `NotebookEntry`, `SourceRecord`, `ArtifactRecord`, `AsyncTask`, `OperationLogEntry`\n2. **運行時型別**（只在記憶體）：`TabHandle`, `NetworkHealth`\n3. **協定型別**（MCP 回應格式）：`TaskNotificationPayload`, `AsyncSubmitResult`, `DaemonStatusResult`\n\n注意 `TabHandle`（第 133 行）直接引用 puppeteer-core 的 `CDPSession` 和 `Page`。這是有意的設計：TabHandle 是 runtime-only，不會被序列化。\n\n## 故障模式\n\n如果 runtime 型別（如 `TabHandle`）被放進持久化邏輯（如 `DaemonState`），JSON.stringify 會爆掉——`CDPSession` 是 circular reference。型別分類就是防止這件事。\n\n## 設計取捨\n\n**選擇**：一個大 types.ts 而不是每個模組自己定義  \n**好處**：跨模組一致性靠編譯器保證，改欄位名全域報錯  \n**壞處**：檔案會越來越大，且所有模組都 import 同一個檔案（fan-in）\n\n---\n\n### 這段在幹嘛\n全系統 17 個 interface + 5 個 type alias，從 data-model.md 1:1 對應產出。\n\n### 思考問題\n1. `TabHandle.cdpSession` 和 `TabHandle.page` 為什麼不用 `unknown` 再 cast？直接依賴 puppeteer-core 的好處和代價是什麼？\n這兩者是 puppeteer 拿過來的的 runtime 物件，也只在 runtime 使用，沒有中間型態的必要，\n先確認用 unknown 的意圖是什麼？預期可以改換別的 browser producer or tab mgr ? 但如果目前沒有這個意圖，雖然跨 file 之間使用的 dependency 高，導致被 puppteer 綁死，但目前就是以 puppeteer 為主來做為基底的情況，其實完全可以接受不用 unknown ，省去了 typecast 的問題\n\n2. `AsyncTask` 的 `history: TaskStatusChange[]` 設計成 append-only。如果要支援 undo，你會怎麼改？\n這裡不應該設計 undo；流程上只需要 cancel / stop 處理進行中的任務，已完成的結果若要改變，就由使用者提交新的需求，系統建立新的 task 來處理，而不是回寫舊 task 的 history。"
    },
    {
      "file": "src/shared/config.ts",
      "line": 9,
      "ref": "157bae12d93932e241a337e33763bd307847a9b2",
      "title": "Layer 0: Configuration — 系統常數與路徑工廠",
      "description": "## 機制\n\n所有 magic numbers 和路徑集中管理：\n- **路徑**：`~/.nbctl/` 為根，下分 `state.json`、`tasks/`、`cache/<alias>/`、`profiles/`\n- **網路**：`MCP_PORT=19224`、`MCP_HOST=127.0.0.1`（只監聽 loopback）\n- **限制**：`MAX_TABS=10`、`CONTENT_WORD_LIMIT=500000`\n- **時間**：`DEFAULT_SESSION_TIMEOUT_MS=300000`（5 min）\n- **權限**：`DIR_PERMISSION=0o700`、`FILE_PERMISSION=0o600`\n\n第 85 行 `findChromePath()` 只搜 macOS 路徑。這是 MVP scope 的有意限制。\n\n## 故障模式\n\n`MCP_HOST=127.0.0.1` 是安全邊界。如果改成 `0.0.0.0`，任何人都能連上 daemon 操作你的 Google 帳號。\n\n## 設計取捨\n\n**選擇**：常數用 `as const`，不用環境變數  \n**好處**：編譯期可見，不會有 `undefined` 意外  \n**壞處**：改設定要改 code 重編。Phase 7+ 可能需要 config file\n\n---\n\n### 思考問題\n1. `isValidNotebookUrl()` 只檢查 prefix 不驗 notebook ID 格式。你能構造一個通過驗證但無效的 URL 嗎？\n可以，但進任務就失敗，這邊延伸出一個思考問題，是否要 fail fast，但實務上要不靠 agent 做成本很高，所以就不考慮了\n2. 如果要支援 Linux，`CHROME_CANDIDATES` 需要加什麼路徑？\n這算查表，但這邊延伸的問題是，這個應該要動態可填，或是做 cli 工具查詢自己填入"
    },
    {
      "file": "src/state/state-manager.ts",
      "line": 54,
      "ref": "157bae12d93932e241a337e33763bd307847a9b2",
      "title": "Layer 1: State — Atomic Write 持久化",
      "description": "## 機制\n\n**原子寫入三步**（第 54-60 行）：\n1. `writeFile(tmpPath, data)` — 寫到臨時檔\n2. `chmod(tmpPath, 0o600)` — 設權限\n3. `rename(tmpPath, statePath)` — 原子替換\n\n如果 step 1 寫一半 crash，原檔完好。`rename` 在 POSIX 是原子操作。\n\n三個 state 模組共用這個 pattern：\n- `StateManager` — 單一 `state.json`（daemon 層級）\n- `TaskStore` — `tasks/<taskId>.json`（一 task 一檔）\n- `CacheManager` — `cache/<alias>/sources.json|artifacts.json|operations.json`\n\n## 故障模式\n\n**load→mutate→save 不是 atomic**：如果兩個 `addNotebook()` 同時跑，第二個會覆蓋第一個的結果。Node.js 單線程通常不會觸發，但 `await` 中間如果有 yield point（例如 `load()` 裡的 `readFile`），理論上可能 interleave。\n\n**現況限制**：Scheduler 保證同一 notebook 串行，所以目前不會出問題。但這是一個 latent bug。\n\n## 設計取捨\n\n**選擇**：JSON 檔案 + 原子 rename，不用 SQLite  \n**好處**：零依賴、可讀、可 git 追蹤  \n**壞處**：無 transaction、無 concurrent write safety、整檔讀寫（不能 partial update）\n\n---\n\n### 思考問題\n1. `TaskStore` 為什麼一 task 一檔，而不像 `state.json` 全放一起？（提示：想想 cleanup TTL）\n因為 task 是短暫在 deamon 的一個紀錄，基本需求是時常刪除時常新增，沒有歷史性需求，還有 TTL 定時清除，基於這些理由一個 task 一個 file 都是合理的選擇\n\n2. `chmod(tmpPath, FILE_PERMISSION)` 在 `writeFile` 之後、`rename` 之前。如果 chmod 失敗，最終檔案權限會是什麼？\ntemp file -> 套上 umask 的 執行 deamon 使用者權限"
    },
    {
      "file": "src/tab-manager/tab-manager.ts",
      "line": 42,
      "ref": "157bae12d93932e241a337e33763bd307847a9b2",
      "title": "Layer 2: TabManager — Chrome 生命週期管理",
      "description": "## 機制\n\n`TabManager extends EventEmitter` 管理單一 Chrome instance + 多 tab：\n- `launch()` — puppeteer-core 啟動 Chrome，帶 `userDataDir`（共用認證）\n- `openTab()` — `browser.newPage()` → `page.goto(url)` → `page.createCDPSession()` → `TabHandle`\n- `closeTab()` / `shutdown()` — 清理資源\n- `switchMode()` — headless ↔ headed 切換（關掉重開）\n\n第 71 行 `browser.on('disconnected')` 是 Chrome crash detector。Chrome 掛了就 emit `'disconnected'` event，清空所有 tab。\n\n## 故障模式\n\n**switchMode() 的破壞性**：第 151 行呼叫 `shutdown()` 再 `launch()`。所有已開 tab 的 CDPSession 都會失效。如果有 agent 正在操作中，它的 tool call 會收到 `Target closed` 錯誤。\n\n→ hooks.ts 的 FATAL_PATTERNS 有 `target.*closed`，會觸發 abort。設計上是對的，但使用者會 lost work。\n\n## 設計取捨\n\n**選擇**：Single Chrome + Multi-tab，不是 BrowserPool  \n**好處**：共用 cookie/session（登入一次全 tab 可用），記憶體省  \n**壞處**：Chrome crash = 全部 tab 都沒了。沒有隔離\n\n**邊界**：TabManager 只管 Chrome 和 tab 生命週期。它不知道 NotebookLM、不知道 agent。cdp-helpers.ts 提供低階 CDP 操作（click, screenshot），但 TabManager 不直接呼叫它們。\n\n---\n\n### 思考問題\n1. `MAX_TABS=10` 的上限在 openTab() 第 97 行檢查。如果 Chrome 有 about:blank 預設 tab，會不會算進去？\n 會，我們這邊的計數是自己處理，不是真的去取得 chrome tab 的數量\n\n2. `userDataDir` 共用登入狀態。如果使用者開了另一個 Chrome 用同一個 profile，會發生什麼？ profile 原則上是 lock 狀態。另一個 chrome instance 應該是開不起來，就算可以，共用 profile 是一件很危險不安全的事情\n"
    },
    {
      "file": "src/network-gate/network-gate.ts",
      "line": 57,
      "ref": "157bae12d93932e241a337e33763bd307847a9b2",
      "title": "Layer 2: NetworkGate — Fail-Open 流量閘門",
      "description": "## 機制\n\n**兩個操作**：\n- `acquirePermit()` — 每次 tool call 前呼叫。健康時立刻返回；throttled 時 sleep 到 backoff 過期\n- `reportAnomaly(signal)` — 遇到 429/503/CAPTCHA 時呼叫。觸發指數退避 + jitter\n\n**Fail-open**（第 81-86 行）：`acquirePermit` 的整個 body 包在 try/catch 裡。如果 gate 自己出 bug（例如 sleep 被 reject），結果是放行，不是阻擋。這是 FR-195 的核心原則。\n\n**指數退避**：初始 5s → 10s → 20s → ... → 上限 300s。每次加 ±20% jitter 避免 thundering herd。\n\n## 故障模式\n\n如果把 fail-open 改成 fail-closed（throw instead of catch），一個 gate bug 就能讓整個 daemon 停擺。這不是假設——setTimeout 在 fake timer 測試環境下的行為不同，如果 gate 依賴精確計時，測試和生產可能不一致。\n\n## 設計取捨\n\n**選擇**：全域 singleton gate，不是 per-notebook  \n**好處**：NotebookLM 只有一個 rate limit（全站共用），一個 gate 就夠  \n**壞處**：如果未來要支援多個目標服務，需要拆分\n\n---\n\n### 思考問題\n1. `recentLatencyMs` 永遠返回 0（第 124 行）。如果要追蹤真實延遲，你會把測量點放在哪個 hook？\n最真實的測量點其實應該在 tab 操作呼叫開始，但其實這裡要先定義好我們要的 latency 是網路呼叫，還是操作流程，另外 recentLatencyMs 如果暫時沒有作用，應該是 null 而不是 0，在語意上才不會混淆\n\n2. `reset()` 是 public method，只標註 \"for testing\"。如果 production code 呼叫它會怎樣？\n如果真的遇到 ratelimit ，就會直接放行，但最後可能被 ban ??"
    },
    {
      "file": "src/agent/client.ts",
      "line": 31,
      "ref": "157bae12d93932e241a337e33763bd307847a9b2",
      "title": "Layer 3: CopilotClient — Agent Runtime 入口",
      "description": "## 機制\n\n`CopilotClientSingleton` 包裝 `@github/copilot-sdk` 的 `CopilotClient`：\n- Daemon 啟動時 `getInstance().start()` — 啟動 Copilot CLI process\n- 每個 task 用 `getClient().createSession({tools, customAgents, hooks})` — 建立 agent session\n- Daemon 關閉時 `stop()` — 關閉 CLI process\n\n**autoRestart**（第 22 行）：SDK 內建的 process restart。但 `_handleUnexpectedExit()`（第 158 行）是我們自己的 restart 邏輯，用 `intentionalStop` 和 `restartInProgress` 防止重複 restart。\n\n## 故障模式\n\n**⚠️ _handleUnexpectedExit 沒有接線**：第 157 行的 comment 說「production code wires this up via SDK lifecycle events」，但實際上沒有任何 code 將它綁到 SDK 的 exit event。目前只有測試會呼叫它。這是 Phase 2 skeleton 的已知缺口——Phase 3 daemon entry point 需要補上。\n\n## 設計取捨\n\n**選擇**：Singleton pattern，不是 DI  \n**好處**：全 daemon 一個 client，簡單。`resetInstance()` 讓測試容易 mock  \n**壞處**：Singleton 是 global state，多測試並行時需要 `resetInstance()`\n\n**邊界**：CopilotClient 只管 CLI process lifecycle。它不知道 tools、不知道 agent configs。Session 的建立和執行是 session-runner 的事。\n\n---\n\n### 思考問題\n1. `getClient()` 在 `!this.started` 時 throw。但 `isRunning()` 還會檢查 `client.getState() === 'connected'`。兩者不一致的時候代表什麼狀態？\n根據下列 insight ，我們必須先確認設計兩套 state 的用意，如果是無心之過，原則上以內部的連線狀態為主就好了，這邊 singleton 就當作是這個 deamon 只能叫一個client 的制約\n\n2. 如果 `autoRestart: true` 和 `_handleUnexpectedExit` 同時跑，會不會重複 restart？\n根據 insight ，copilotclient source code 沒有保證 auto restart 期間 start 不會重複執行，所以這邊如果要接上，必須要考量這個問題\n\n### review insight\n我先檢查這個 singleton 在專案內的使用方式和測試，確認 `started` 與 SDK state 的設計意圖，再直接回答這兩個疑問。\n\nSearched for regex `started|isRunning\\(|getClient\\(|_handleUnexpectedExit|autoRestart|getState\\(` (`**/src/**`), 33 results\n\nSearched for regex `CopilotClientSingleton|getInstance\\(|resetInstance\\(|isRunning\\(|_handleUnexpectedExit|autoRestart|getState\\(` (`**/tests/**`), 60 results\n\nRead [](file:///Users/rickwen/code/notebooklm-controller/src/agent/client.ts)\n\n我已確認目前實作確實同時維護兩個來源的狀態，下一步看測試是否把這種「雙軌 state」當成刻意設計，還是只是暫時包裝。\n\nSearched for regex `getClient\\(\\)|isRunning\\(\\)|start\\(\\)|stop\\(\\)` (`**/src/**`), 24 results\n\nRead [](file:///Users/rickwen/code/notebooklm-controller/tests/unit/agent/client.test.ts)\n\nRead [](file:///Users/rickwen/code/notebooklm-controller/src/agent/session-runner.ts)\n\n測試把 `started` 視為 wrapper 自己的生命週期旗標，不是單純 mirror SDK state。我要再補看規格和依賴層是否有明確說明 `autoRestart` / `getState()` 的語意，避免只靠推測。\n\nSearched for regex `autoRestart|getState\\(|CopilotClientSingleton|client.start\\(|client.stop\\(` (`**/specs/**`), 5 results\n\nSearched for regex `autoRestart|getState\\(|on\\(|process exit|unexpected exit|CopilotClientSingleton` (`**/docs/**`), 2 results\n\nRead [](file:///Users/rickwen/code/notebooklm-controller/package.json)\n\nSearched for regex `on\\(|exit|disconnect|connected|disconnected` (`**/src/agent/**`), 24 results\n\nSearched for regex `client.getState\\(|state === \"connected\"|connected` (`**/tests/unit/agent/client.test.ts`), 17 results\n\nRead [](file:///Users/rickwen/code/notebooklm-controller/specs/001-mvp/research.md#68-68), lines 68 to 96\n\n我補了一個關鍵點：目前 repo 內看不到任何地方把 `_handleUnexpectedExit()` 真正註冊到 SDK event，所以它現在比較像「測試用鉤子」，不是已經在 production 跑的重啟路徑。\n\nSearched for regex `_handleUnexpectedExit\\(|\\.on\\(|onProcessExit|processExit|unexpected exit|unexpectedExit` (`**/src/**`), 5 results\n\nSearched for regex `autoRestart|_handleUnexpectedExit` (`**/docs/**`), no results\n\nSearched for regex `autoRestart|_handleUnexpectedExit` (`**/specs/**`), 4 results\n\n我再看一下已安裝的 SDK 型別或原始碼，確認 `getState()` 可能有哪些值，以及 `autoRestart` 是否真的代表 SDK 內部會自己啟動 restart loop。\n\nSearched for regex `type .*State|getState\\(|autoRestart|connected|disconnected` (`**/node_modules/@github/copilot-sdk/**`), 73 results\n\nSearched for files matching `**/node_modules/@github/copilot-sdk/**/*.{d.ts,ts,js}`, no matches\n\nSDK 原始碼有明確訊號了：`ConnectionState` 是 `disconnected | connecting | connected | error`，而且 SDK 自己很多 API 會以「是否 connected」當 guard。我要看 `start()` 和 `autoRestart` 的實際條件，確認你第二點。\n\nRead [](file:///Users/rickwen/code/notebooklm-controller/node_modules/%40github/copilot-sdk/dist/client.d.ts#170-170), lines 170 to 205\n\nRead [](file:///Users/rickwen/code/notebooklm-controller/node_modules/%40github/copilot-sdk/dist/client.js#165-165), lines 165 to 210\n\nRead [](file:///Users/rickwen/code/notebooklm-controller/node_modules/%40github/copilot-sdk/dist/client.js#880-880), lines 880 to 915\n\nRead [](file:///Users/rickwen/code/notebooklm-controller/node_modules/%40github/copilot-sdk/dist/client.js#990-990), lines 990 to 1010\n\nSearched for regex `async reconnect\\(|reconnect\\(\\)` (`**/node_modules/@github/copilot-sdk/dist/client.js`), 3 results\n\nRead [](file:///Users/rickwen/code/notebooklm-controller/node_modules/%40github/copilot-sdk/dist/client.js#250-250), lines 250 to 340\n\nRead [](file:///Users/rickwen/code/notebooklm-controller/node_modules/%40github/copilot-sdk/dist/client.js#1148-1148), lines 1148 to 1169\n\nRead [](file:///Users/rickwen/code/notebooklm-controller/node_modules/%40github/copilot-sdk/dist/client.js#340-340), lines 340 to 390\n\n1. 你對第一點的直覺是對的，這個 wrapper 現在有「兩個不同語意的 state」混在一起，而且沒有完全對齊。\n\n在 client.ts 裡，`isRunning()` 定義的是「wrapper 認為已啟動，而且 SDK 目前真的 `connected`」。但在 client.ts 裡，`getClient()` 只看 `started`，不看 SDK state。這代表會出現這些狀態：\n\n- `started = true`，但 `client.getState() !== \"connected\"`：最合理的意思是「這個 singleton 曾經成功啟動過，但現在連線已掉了、正在 reconnect、或進入 error」。SDK 的 state 實際上有 4 種：`disconnected | connecting | connected | error`，不是只有 connected/disconnected，定義在 types.d.ts。\n- `started = false`，但 SDK 還是 `connected`：在這份 code 裡比較不像正常設計狀態，比較像 wrapper 與 SDK 漂移了。你說的「我們停了但 connection 還沒處理好」屬於這一類，但以目前流程看，這應該只會是暫時 race 或設計缺口，不應該被當作穩定語意。\n\n你質疑「為什麼不直接用 SDK state 當唯一依據」是合理的。現在 `started` 真正提供的價值只有兩個：\n\n- 表達 app lifecycle intent: 我們有沒有明確呼叫過 `start()` / `stop()`\n- 做 double-start guard 與 intentional-stop 判斷\n\n但如果它被拿來當可用性判斷，就會跟 SDK 真實連線狀態打架。尤其 session-runner.ts 是直接 `client.getClient()` 後再 `createSession()`，所以當 `started = true` 但 SDK 其實已斷線時，wrapper 會放行，最後失敗點落到 SDK 內部。\n\n你提的另一個疑問要拆開看：`start()` 失敗會不會還把 `started` 設成 `true`？這一點倒不是 bug。因為 client.ts 是先 `await this.client.start()`，成功後才 `this.started = true`。而 SDK 自己的 `start()` 只有在完成 connect 與 protocol verify 後才會設成 `connected` 並 resolve，失敗會設成 `error` 然後 throw，見 client.js。所以「start promise reject 但 wrapper 還設 true」這件事在目前實作不會發生。真正的問題是：成功之後，兩份 state 之後仍可能漂移。\n\n所以結論很直接：\n目前這份 code 把 `started` 當成 lifecycle flag 可以，但把它當 `getClient()` 的唯一 guard，不夠嚴謹。比較一致的做法通常是：\n\n- 要嘛只信 SDK state，把 wrapper state 縮到最小\n- 要嘛保留 `started`，但明確定義它只是「desired/managed lifecycle」，而 `getClient()` / readiness 判斷一律用 `isRunning()` 或直接用 SDK `getState()`\n\n2. 你第二點的結論，在這個 repo 的現況下不成立得那麼強。\n\n先講現況：目前 repo 內看不到任何地方把 client.ts 的 `_handleUnexpectedExit()` 註冊到 SDK event，所以現在真正會跑的 restart 路徑只有 SDK 自己的 `autoRestart`。測試有直接呼叫 `_handleUnexpectedExit()`，見 client.test.ts，但那是測試 hook，不是 production wiring。\n\n再講 SDK 本身：`autoRestart` 的確是 SDK 內建 restart。它會在 child process exit 或 connection close 時觸發 `reconnect()`，見 client.js 和 client.js。而 `reconnect()` 會自己做 `stop()` 再 `start()`，見 client.js。\n\n所以如果未來你真的把 `_handleUnexpectedExit()` 也接上去，而且還保留 SDK `autoRestart: true`，那就不能直接假設「SDK 內部應該會處理好衝突」。因為：\n\n- wrapper 的 `restartInProgress` 只能防 wrapper 自己重入\n- 它擋不住 SDK 內部那條 `reconnect()` 路徑\n- 兩邊都可能各自呼叫 `start()` / `stop()`\n\n也就是說，若兩條路徑同時啟用，理論上是有重複 restart 風險的，除非 SDK 文件明確保證這種外部重啟與內部重啟是可重入且去重的。從目前看到的程式碼，我看不到這個保證。\n\n更精準的判斷是：\n\n- 現在這個 repo：不會雙重 restart，因為 `_handleUnexpectedExit()` 根本還沒接上 production\n- 如果之後接上：有風險，不該靠猜測說「原則上內部應該會處理好」\n\n如果你要把這段設計收斂，我會建議二選一：\n\n1. 完全信 SDK 的 `autoRestart`，刪掉或至少不接 `_handleUnexpectedExit()` 的 restart 職責。\n2. 關掉 `autoRestart`，由 wrapper 全權管理 restart，並把 `started` 明確改成 lifecycle-intent，不再跟 readiness 混用。\n\n如果你要，我下一步可以直接幫你整理成一版「這段設計的問題清單 + 建議重構方向」，或逐行 review client.ts。"
    },
    {
      "file": "src/agent/hooks.ts",
      "line": 101,
      "ref": "157bae12d93932e241a337e33763bd307847a9b2",
      "title": "Layer 3: SessionHooks — Agent 生命週期四鉤子",
      "description": "## 機制\n\n`createSessionHooks(context)` 回傳四個 hook：\n\n1. **onPreToolUse** — 每次 tool call 前：`acquirePermit()` + structured log\n2. **onPostToolUse** — tool call 後：log 結果\n3. **onErrorOccurred** — 錯誤分類三路由：\n   - `retry`（429, timeout, network）→ 最多重試 3 次\n   - `skip`（element not found）→ 跳過繼續\n   - `abort`（auth expired, Chrome crash）→ 立即終止\n4. **onSessionEnd** — 記錄 session 時長\n\n**SDK hook types 必須自定義**（第 13-26 行）：`@github/copilot-sdk` 不 re-export hook 相關的 interface。我們在本地定義了 `PreToolUseHookInput`、`PostToolUseHookInput` 等。這是從 SDK source code 反推出來的。\n\n## 故障模式\n\n**classifyError 的 default 是 abort**（第 94 行）：未知錯誤 = 終止。這是保守策略——寧可 abort 也不要 silent data loss。但如果 SDK 內部拋了一個我們沒分類的 error message，就會意外中斷。\n\n## 設計取捨\n\n**選擇**：Regex pattern matching 分類錯誤，不是 error code  \n**好處**：不依賴 Google 返回特定 error code（它不會）  \n**壞處**：Brittle — Google 改 error message 就會分類錯誤。需要持續更新 pattern\n\n---\n\n### 思考問題\n1. `onPreToolUse` 裡 `acquirePermit()` fail-open 後 tool 照跑。如果 tool 遇到 429，`onErrorOccurred` 又會 `reportAnomaly`。這形成一個 feedback loop — 你能畫出完整的流程嗎？\n\n\n2. FATAL_PATTERNS 包含 `session.*closed`。但 MCP session closed 和 Copilot session closed 是不同的。這個 pattern 會不會誤殺？\n\n\n## 實作缺失回報\nFinding 1: Task plan currently leaves FR-051 under-specified and mis-accounted across layers\n\nThe most serious issue in the current plan is that agent execution structured logs and client-facing operation logs are being treated as if they can be completed by the same downstream work, but the spec explicitly defines them as different layers with different owners and different granularity. As written, the completed Phase 2 tasks only establish logger infrastructure and minimal hook wiring, while the only later logging-related follow-up task, T095, is for OperationLogEntry and cannot close the FR-051 gap.\n\nEvidence\n\nFR-051 requires structured logs for every agent operation step, including enter/exit timing, tool calls, screenshot events, and errors, with correlation fields for diagnosis: spec.md:1216, spec.md:1220\nThe data model explicitly separates three recording layers and says they must not be merged:\ndata-model.md:184, data-model.md:190\nOperationLogEntry is defined as one record per exec call, written after session end by the daemon, which makes it a client job ticket log rather than agent execution telemetry:\ndata-model.md:136, data-model.md:141\nThe task plan marks the logger infrastructure and SessionHooks work as complete, but those tasks only cover the minimal path:\nT007.1, tasks.md:70, tasks.md:71\nThe only later follow-up is T095, which is explicitly OperationLog recording in session-runner, not agent step logging:\ntasks.md:295\nThe current hook implementation confirms the gap: it logs only coarse lifecycle events with thin payloads.\nhooks.ts:123, hooks.ts:152, hooks.ts:171, hooks.ts:198\nCurrent tests only verify basic correlation logging and session-end duration, not FR-051-grade diagnosability:\nhooks.test.ts:181, hooks.test.ts:429\nWhy this is severe\n\nThis is not just an implementation quality issue. It is a planning defect:\n\nPhase 2 can be declared complete while a foundational observability requirement is still materially unfinished.\nFuture work is likely to drift toward overloading OperationLogEntry to compensate for missing execution telemetry, which would violate the model in data-model.md:184.\nOnce browser-driven flows start in US1 through US10, the project will lack the diagnostic depth needed to explain hangs, retries, tool latency, and UI-state failures.\nRecommended correction\n\nThe plan should add an explicit follow-up task for FR-051 agent execution logging, separate from T095, and place it in Phase 2 or immediately before Phase 3. Suggested shape:\n\nAdd a test task for SessionHooks observability in hooks.test.ts\nScope: per-tool start/end, per-tool duration, sanitized tool argument summary, result summary, error classification metadata, session counters, screenshot/tool event coverage\nAdd an implementation task for FR-051 execution structured logs in hooks.ts\nScope: session-local timing/state, richer structured events, correlation continuity, per-tool diagnostics, session aggregate summary\nCodetour-ready summary\n\nPhase 2 currently marks structured logging as complete, but only the logger base and minimal hook wiring were implemented. The spec and data model define Structured Logs and OperationLogEntry as separate layers: agent step telemetry versus one-entry-per-exec client history. Because the plan has no dedicated task after T026 to raise SessionHooks to FR-051 diagnostic depth, and the only later logging task is T095 for OperationLogEntry, the roadmap leaves agent execution observability materially unfinished while appearing complete. This is the most serious planning issue found so far because it creates a false sense of foundational completeness and will make later browser-flow debugging substantially harder."
    },
    {
      "file": "src/agent/session-runner.ts",
      "line": 61,
      "ref": "157bae12d93932e241a337e33763bd307847a9b2",
      "title": "Layer 3: Session Runner — 單次 Task 執行",
      "description": "## 機制\n\n`runSession(options, prompt)` 是每個 task 的執行單元：\n1. `client.getClient()` → 拿到 SDK client\n2. `client.createSession({tools, customAgents, hooks, onPermissionRequest})` → 建 session\n3. `session.sendAndWait({prompt}, timeoutMs)` → 送 prompt 等結果\n4. `finally` → `session.disconnect()` 釋放資源\n\n**Permission handler**（第 48 行）：`autoApprove` 返回 `{ kind: 'approved' }`。Copilot SDK 的 tool execution 會問 permission，我們全部自動通過。安全靠三道防線（tool 白名單 + handler 只做我們寫的事 + agent prompt 約束），不靠 permission dialog。\n\n## 故障模式\n\n**timeout 沒有用 Promise.race**：第 102 行 `sendAndWait({prompt}, timeoutMs)` 把 timeout 交給 SDK 處理。如果 SDK 的 timeout 實現有 bug，我們沒有外層保護。Phase 3 可考慮加一個外層 `Promise.race`。\n\n## 設計取捨\n\n**邊界**：session-runner 不知道 task 是什麼、不管 task store、不做 retry。它只是 `createSession → sendAndWait → disconnect` 的封裝。Retry 和 task lifecycle 是 scheduler + hooks 的責任。\n\n---\n\n### 思考問題\n1. `response?.data?.content ?? undefined` — 如果 SDK 改了 response 結構（例如 `response.content` 而不是 `response.data.content`），會靜默返回 `undefined` 而不是報錯。你會怎麼做 response validation？\n2. `finally` 裡 `session.disconnect()` 失敗只 log warning。如果 disconnect 永遠 hang，daemon 會洩漏 session 嗎？\n\n## Review\nSession Runner Review Notes\n\n目前對 session-runner.ts:61 的結論，不是在否定 SPEC / PLAN 採用官方 Copilot SDK best practice，而是在指出這個模組作為 SDK 邊界層，現階段缺少最基本的 boundary validation。\n\n1. 目前已做的保護\n\nsession-runner.ts:76 有基本的 exception handling。\nclient.getClient()、createSession()、sendAndWait() 若 throw，都會進入 catch，回傳 success: false。\nsession-runner.ts:133 的 finally 會嘗試 session.disconnect()，disconnect reject 時只記 warning，不會再往外 throw。\n所以這段不是完全沒有 error handling，而是只有處理 exception-level failure。\n2. 主要問題不是 catch 太少，而是 outcome model 太扁\n\nsession-runner.ts:102 到 session-runner.ts:114 幾乎等同於：\nSDK throw -> failure\nSDK 不 throw -> success\n這是一種二元 outcome model，只看控制流，不看契約與語意。\n目前缺的是：\ncontract validation：SDK response shape 是否符合預期\nsemantic interpretation：shape 合法是否真的代表 task 成功\ndiagnostics：除了 success/failure 外，缺少可供 debug 的結果分類與上下文\n3. success: true 條件過寬，這是 MVP 也不該接受的點\n\nsession-runner.ts:113 無條件回傳 success: true，只要 sendAndWait() 沒 throw。\nsession-runner.ts:114 直接讀 response?.data?.content ?? undefined。\n這代表：\nresponse shape drift 可能被當成成功\nunexpected event type 可能被當成成功\nempty / partial / malformed payload 可能被當成成功\n最後落成 success: true, result: undefined\n這不是單純「可再加強」，而是 boundary validation 缺失，屬於 MVP 應補齊的最低線。\n4. 更精確的說法\n\n不要只用「這段沒做 error check」描述，較精確的說法是：\n\n有 exception-level error check\n幾乎沒有 contract-level validation\n幾乎沒有 semantic-level outcome modeling\ndiagnostics 也偏弱，不利 debug 與後續穩定化\n5. runSession 目前尚未接進 production scheduler flow\n\nTrace 結果顯示：\n\nsession-runner.ts:61 的 runSession() 在 workspace 內目前只有 unit test 直接呼叫，見 session-runner.test.ts:107\nscheduler.ts:27 的 Scheduler 是透過 dependency injection 接收 runTask\n但目前 repo 內沒有找到 production code 去 new Scheduler({ runTask: ... }) 並把 runSession 包進去\nscheduler 目前主要存在於自己的 unit test，見 scheduler.test.ts:53\n結論：目前 session-runner 與 scheduler 是分別完成的模組骨架，但 production wiring 尚未落地。\n\n6. disconnect() hang 的影響比單純 cleanup failure 更嚴重\n\n如果 session-runner.ts:136 的 await session.disconnect() 永遠不 resolve：\n\nrunSession() 不會返回\n同 notebook queue 會被卡住，因為 scheduler 是 per-notebook serial execution，見 scheduler.ts:214 與 scheduler.test.ts:111\nwaitForIdle() / shutdown() 也可能被拖住，見 scheduler.ts:174 與 scheduler.ts:185\n如果底層 session / client 資源未釋放，可能進一步演變成全域資源壓力問題\n所以 disconnect 問題不是只有 log warning 那麼簡單，若是 hang 而不是 reject，影響層級會升高。\n\n7. 這次 review 的核心判斷\n\n這不是「不信任官方 SDK」，而是典型的 trust boundary / boundary management 問題：\n\n架構層可以合理依賴官方 SDK best practice\n但 adapter / boundary layer 不能只依賴，還必須驗證、分類、隔離、觀測\n一句話總結：\n\n目前 session-runner 有基本 exception handling，但缺少 MVP 也應具備的 response validation 與 outcome interpretation；此外它目前尚未接入 scheduler 的 production flow，而 disconnect() hang 的潛在影響會從單一 task 擴散到 notebook queue 與 shutdown path。\n\n8. 建議後續 review 重點\n\n定義 sendAndWait() 回傳的專案內可接受 shape，補最小 validation。\n區分 SDK throw、invalid response、empty response、cleanup anomaly 等不同 outcome。\n檢查 disconnect() 是否需要外層 timeout / health escalation。\n補上 runSession -> runTask -> Scheduler 的 production wiring trace。\n確認 SessionResult 是否需要比現在更能表達 diagnostics 與 degraded success。"
    },
    {
      "file": "src/agent/tools/browser-tools.ts",
      "line": 54,
      "ref": "157bae12d93932e241a337e33763bd307847a9b2",
      "title": "Layer 3: Browser Tools — Vision Agent 的手和眼",
      "description": "## 機制\n\n`createBrowserTools(tabHandle)` 產出 5 個 tool：\n- **screenshot** — `Page.captureScreenshot` → base64 image via `binaryResultsForLlm`\n- **click** — `Input.dispatchMouseEvent` (pressed + released) → **auto screenshot**\n- **type** — `Input.dispatchKeyEvent` (keyDown + keyUp per char)\n- **scroll** — `Input.dispatchMouseEvent` (mouseWheel) → **auto screenshot**\n- **paste** — `Input.insertText` (bypass keyboard events)\n\n**Tool 自包原則**：click 和 scroll 自動回傳 screenshot（第 98-103 行、第 136-141 行）。Agent 不需要額外呼叫 screenshot tool 來看操作結果，減少 tool call 來回。\n\n每個 tool 用 `defineTool(name, { description, parameters: z.object(...), handler })` 定義。Zod schema 自動轉成 JSON Schema 給 LLM 看。\n\n## 故障模式\n\n**座標精度**：agent 看到的是 screenshot，但 Chrome viewport 可能有 devicePixelRatio > 1。如果 screenshot 是 2x 解析度但座標是 1x，點擊會偏移。cdp-helpers 直接用 CSS pixel 座標，所以只要 screenshot 也是 CSS pixel 就沒問題。\n\n## 設計取捨\n\n**選擇**：原始 CDP command，不用 Puppeteer 的 `page.click()`  \n**好處**：精確控制，不依賴 Puppeteer 的 selector-based 點擊（NotebookLM 沒有穩定 selector）  \n**壞處**：沒有 auto-scroll-into-view、沒有 actionability check\n\n**邊界**：browser-tools 拿到的是 `TabHandle`，從中取 `cdpSession`。它不管 tab 怎麼開的、不管 Chrome 怎麼啟動的。\n\n---\n\n### 思考問題\n1. type tool 是逐字元 keyDown/keyUp。如果要輸入 1000 字的長文本，會有多少個 CDP call？paste tool 的存在是為了解決這個問題嗎？\n2. `as any as Tool<unknown>[]`（第 160 行）是因為 Zod 推導出的 `Tool<{x: number, y: number}>` 不能直接 assign 給 `Tool<unknown>[]`。這是 TypeScript 的 covariance 問題——你能解釋為什麼嗎？\n\n## review\n**結論報告**\n\n目前的共識是：主專案架構方向暫時不翻動，但要立刻補上一條獨立的實驗支線，用來驗證「Copilot SDK + browser tools + NotebookLM 真實互動」到底能不能穩定成立。這條支線不是產品開發，而是風險關閉用的 spike。它的目的不是把所有主線功能提前做完，而是把目前最大的不確定性，從主排程中切出來單獨驗證。\n\n這個決策的原因很清楚。現有 foundation 在設計與模組分工上大致可行，主線 task 也已經有明確切分，見 tasks.md 與 plan.md。但功能面仍有一個高風險假設尚未被證明：agent 拿到 screenshot 與 click/type/paste 等工具後，是否真能在 NotebookLM 上穩定完成關鍵操作。這件事在現況中只做到 mock/unit 級別，還沒有完成 runtime 驗證；同時專案文件本身也承認有 wait-and-see 與 spike 待驗證項目，見 research.md 與 browser-state.md。因此，現在最合理的做法不是重切整個主線 phase，也不是忽略風險往下做，而是另開一個實驗小專案或 spike workspace，集中驗證核心互動能力。\n\n**決策摘要**\n\n1. 主專案排程不做大幅重排。\n2. 另外拉出一條 spike 支線，專門做 browser-agent capability experiment。\n3. spike 允許更高自由度的實作與試錯，不必完全受主專案 spec 與 constitution 綁住。\n4. 但 spike 仍應盡量維持與主專案核心技術一致，尤其 agent runtime 優先維持 Copilot SDK，方便結論回灌。\n5. spike 驗證完成後，再回頭決定哪些工具設計、prompt protocol、route abstraction 應納入主專案。\n\n**Spike 的驗證範圍**\n\n這條支線至少驗證三條代表性 flow，不碰那種等待時間過長的操作：\n\n1. 建立筆記\n2. 提問\n3. 加入來源\n\n這三條 flow 已足以覆蓋最重要的互動風險：畫面辨識、目標定位、點擊命中、輸入可靠性、狀態切換、多步驟 modal 流程，以及操作後驗證。若這三條能穩定重跑，主專案後續 user story 就有足夠信心往下做。\n\n**Spike 的雙軌工作方式**\n\n這次共識不是只做單一實驗，而是兩條 lane 平行推進。\n\n1. Capability lane\n目標是驗證 agent 拿到工具後，能否在真實 NotebookLM 上穩定完成 create notebook、query、add source。\n\n2. Route lane\n目標是把成功跑過的高頻互動片段沉澱成 reusable route tools 或 route library，避免未來每次都從零開始靠 vision 推理。\n\n第二條 lane 很重要。因為純靠 screenshot 從零推理每一步，雖然可行，但太慢也太脆弱。你提到購票系統的做法是先把穩定路徑沉澱下來，這個原則在 NotebookLM 也成立。只是 route 不應做成死座標腳本，而應做成帶 checkpoint 的 semantic route。也就是說，收集的是「打開新增來源入口」、「切到 copied text 模式」、「聚焦 query 輸入框」、「送出 query」這類可重用的互動片段，而不是單純記錄某個 x,y 點擊。\n\n**對主專案的影響**\n\n這個做法不會和現有架構衝突，反而是補強它。現有主專案已經把工具註冊、tool registry、agent config 外部化的延展空間留好了，見 index.ts 與 agent-loader.ts。也就是說，若 spike 證明某些 route 很值得固化，之後完全可以回灌成新的高階工具或 agent 專用工具，而不必推翻主線設計。\n\n真正要避免的是兩件事：\n\n1. 為了驗證核心能力，現在就把主專案 phase 全部重切，造成大量依賴提前展開。\n2. spike 做成完全脫離主專案的 demo，最後沒有任何可回收結論。\n\n所以正確姿勢是：主線維持、spike 抽離、結論回灌。\n\n**Spike 的建議驗收標準**\n\n這條支線完成時，不應只說「看起來成功」，而要有明確驗收結果。\n\n1. Create notebook flow 可重跑成功\n2. Query flow 可重跑成功\n3. Add source flow 可重跑成功\n4. 每條 flow 有成功率、耗時、失敗樣態紀錄\n5. 至少沉澱出一批可重用的 route tools\n6. 最後輸出主專案回灌建議：哪些做法保留、哪些調整、哪些放棄\n\n**應回灌主專案的成果類型**\n\n如果 spike 跑出成果，回灌主專案時應優先吸收這幾類東西：\n\n1. 更好的 browser tool 設計\n例如帶 metrics 的 screenshot、點位檢查、checkpoint 驗證工具。\n\n2. 更好的 agent 操作協議\n例如先描述目標、再定位、再執行、再驗證，而不是直接盲點。\n\n3. 高頻 route tools\n把穩定的交互片段收成語意化工具，降低後續 prompt 與 tool call 複雜度。\n\n4. 可靠性結論\n例如哪些模式在 headed/headless 下都穩，哪些會受 viewport、DPI、render timing 影響。\n\n**最終判斷**\n\n這次的結論不是「主線要停」，也不是「主線要重做」。而是：\n\n主線繼續照原本架構推進，但立即補一條高自由度的 spike 支線，專門驗證核心功能可行性與收集可重用操作路徑。這是目前成本最低、風險控制最合理、也最能保留主專案節奏的做法。\n\n如果你要，我下一步可以直接幫你整理第二份文件：spike brief。內容會包含目標、邊界、雙 lane 工作範圍、驗收標準，以及建議第一批 route tools。\n\n## 實驗建議\n可以，下面是精簡版報告，刻意只保留決策、邊界與驗收，讓後續 Claude Code 還有發揮空間。\n\n**結論**\n\n主專案不大改 phase，不為了驗證而重切整個 task。  \n另外拉一條獨立 spike 支線，專門驗證「Copilot SDK + browser tools + NotebookLM 真實互動」是否可靠。\n\n**為什麼這樣做**\n\n目前主架構方向可行，但功能驗證不足。  \n最大的風險不是模組設計，而是 agent 拿到 screenshot、click、type、paste 後，能不能真的穩定完成 NotebookLM 操作。  \n這個風險現在不適合直接塞回主線 phase 裡解，成本太高；但也不能繼續當成假設往下推。\n\n**Spike 目標**\n\n只回答一件事：  \n這套 agent/browser interaction 能不能在真實 NotebookLM 上穩定 work。\n\n**Spike 範圍**\n\n驗證 3 條代表性 flow：\n\n1. 建立筆記\n2. 提問\n3. 加入來源\n\n避開需要長時間等待的操作，不追求完整產品功能。\n\n**工作方式**\n\n兩條 lane：\n\n1. Capability lane\n直接驗證 agent + tools 能否把 flow 跑通。\n\n2. Route lane\n把成功跑過的高頻互動片段整理成可重用 route / tool，避免每次從零開始操作。\n\n**原則**\n\n1. spike 可以自由試做法，不必完全受主專案 spec/constitution 限制。\n2. 但盡量保留與主專案相同的核心前提，特別是 Copilot SDK。\n3. 不做大而全 prototype，只做高風險能力驗證。\n4. route 不做死座標腳本，盡量做成有 checkpoint 的可重用操作片段。\n\n**驗收**\n\n通過條件：\n\n1. 三條 flow 都能重跑成功\n2. 有基本成功率與失敗樣態紀錄\n3. 能整理出一批值得回灌主專案的 route / tool\n4. 最後能回答：\n這條技術路線是否可繼續\n哪些工具該補\n哪些操作該抽成高階 route\n\n**對主專案的影響**\n\n1. 主線照原排程走\n2. 不先大改 tasks phase\n3. spike 結果再回灌主專案調整 tool / prompt / route 設計\n\n**一句話版本**\n\n主線不重排，另外開 spike；先驗證 create notebook、query、add source 是否可靠，再把成功互動沉澱成 route/tool，最後回灌主專案。"
    },
    {
      "file": "src/daemon/scheduler.ts",
      "line": 39,
      "ref": "157bae12d93932e241a337e33763bd307847a9b2",
      "title": "Layer 4: Scheduler — Per-Notebook 任務調度",
      "description": "## 機制\n\n**兩條規則**：\n1. **同 notebook 串行**：FIFO queue，一個做完才做下一個（NotebookLM 同一 notebook 不能並行操作）\n2. **跨 notebook 並行**：不同 notebook 的 queue 獨立 process（它們是不同 tab）\n\n`submit()` → 入隊 → `ensureProcessing()` 啟動 loop → `processQueue()` 逐個 `executeTask()`。\n\n**取消設計**（第 103-148 行）：\n- 排隊中的 task：直接從 queue 移除 + transition to `cancelled`\n- 執行中的 task：設 `cancellationFlag.cancelled = true`，task 完成後檢查\n\n**依賴注入**：`runTask` 是注入的函式，不是直接呼叫 session-runner。這讓 Scheduler 可以純粹測試而不 mock SDK。\n\n## 故障模式\n\n**⚠️ result/error 沒有 persist**（第 296-314 行）：`executeTask` 呼叫 `taskStore.transition()` 轉狀態後，把 `result` 和 `error` 設在返回的 task 物件上，但沒有再呼叫一次 `writeTask` 存回去。下次 `taskStore.get()` 拿到的 task 不會有 result/error。\n\n→ 這是 **Phase 2 的 bug**，Phase 3 需要修。\n\n## 設計取捨\n\n**選擇**：記憶體 queue（Map<string, AsyncTask[]>），不是持久化 queue  \n**好處**：快、簡單  \n**壞處**：daemon restart 會遺失排隊中的 task。但 task 本身已經持久化在 TaskStore，只是 queue 順序沒了\n\n---\n\n### 思考問題\n1. `shutdown()` 等所有 processing loop 結束，但不會中斷正在跑的 `runTask()`。如果 runTask 要 5 分鐘，shutdown 就要等 5 分鐘。你會怎麼設計 graceful shutdown with deadline？\n2. `onTaskComplete` callback 在 task 完成時觸發——這是 notification 的接入點。為什麼不直接在 Scheduler 裡 import Notifier？"
    },
    {
      "file": "src/daemon/mcp-server.ts",
      "line": 52,
      "ref": "157bae12d93932e241a337e33763bd307847a9b2",
      "title": "Layer 5: MCP Server — 對外的 Streamable HTTP 介面",
      "description": "## 機制\n\n`NbctlMcpServer` 是最外層——AI client（Claude, ChatGPT）透過它操作 daemon：\n- `registerTool(name, options, handler)` — 註冊 MCP tool（get_status, exec, shutdown 等）\n- `start()` — 啟動 HTTP server on `127.0.0.1:19224`\n- `handleHttpRequest()` 路由：\n  - `POST /mcp` → JSON-RPC（initialize 或 tool call）\n  - `GET /mcp` → SSE stream（server-push 通知）\n  - `DELETE /mcp` → session 終止\n\n**Session 管理**（第 277-298 行）：每個 initialize request 建一個 `StreamableHTTPServerTransport`，存在 `this.transports` Map 裡。後續 request 靠 `mcp-session-id` header 路由到正確的 transport。\n\n## 故障模式\n\n**⚠️ JSON.parse 沒有 error handling**（第 266 行）：`JSON.parse(body)` 如果 body 不是 valid JSON 會 throw。外層 try/catch 會回 500，但 JSON-RPC spec 要求回 `-32700 Parse error`。\n\n**⚠️ 多 session 共用一個 McpServer**（第 296 行）：每個新 session 呼叫 `this.mcpServer.connect(transport)`。MCP SDK 的 McpServer 設計上是 1:1 連接——connect 第二個 transport 可能 detach 第一個。Phase 3 需要驗證多 session 行為。\n\n## 設計取捨\n\n**選擇**：raw `node:http` + SDK transport，不用 Express  \n**好處**：零額外依賴，完全控制 request lifecycle  \n**壞處**：手動處理 body parsing、routing、error response\n\n**邊界**：MCP Server 只是 transport layer。它不知道 task、不知道 notebook、不知道 Chrome。每個 tool handler 裡才會觸碰業務邏輯（Phase 3 接線）。\n\n---\n\n### 思考問題\n1. `this.transports` 是個不斷增長的 Map。如果 client 斷線但沒送 DELETE，transport 會一直留著。你會怎麼設計 session cleanup？\n2. 為什麼 `MCP_HOST = 127.0.0.1` 而不是 `0.0.0.0`？如果要從遠端 container 連入，你需要改什麼？"
    },
    {
      "file": "src/notification/notifier.ts",
      "line": 31,
      "ref": "157bae12d93932e241a337e33763bd307847a9b2",
      "title": "邊界模組: Notifier — Best-Effort 通知",
      "description": "## 機制\n\n`notify(task)` 發送 `notifications/task-completed` 到 MCP client：\n- **Fire-and-forget**（第 42-52 行）：呼叫 `server.notification()` 但不 await。Promise 的 `.catch()` 只 log warning。\n- **No server = no-op**（第 32-37 行）：如果 MCP server 還沒設定，直接 return。\n\n## 故障模式\n\n通知不保證送達。Client 斷線了、網路斷了、server 還沒 start，都會靜默失敗。\n\n→ 這是 **by design**。任務完成是 daemon 的責任（持久化在 TaskStore），通知是 best-effort。Client 應該用 `get_status` tool poll 來確認（pull > push）。\n\n## 設計取捨\n\n**選擇**：MCP notification（server → client push），不是 WebSocket / polling  \n**好處**：MCP 原生支持，client 不需要額外 protocol  \n**壞處**：單向、不可靠。Client 可能不在線\n\n---\n\n### 思考問題\n1. `buildPayload` 把 `task.status === 'failed'` 映射到 `status: 'failed'`，其他都是 `'completed'`。但 `cancelled` task 也會進來嗎？Scheduler 只在 completed/failed 時觸發 onTaskComplete 嗎？\n2. 如果要加 retry（最多 3 次、間隔 5 秒），你會改 `notify()` 還是在外面包一層？"
    },
    {
      "title": "現況與邊界總結",
      "description": "## Phase 2 現況\n\n```\n┌─────────────────────────────────────────────────────┐\n│                  MCP Client (Claude)                │\n│                        │                            │\n│                   POST /mcp                         │\n│                        ▼                            │\n│  ┌──── Layer 5: MCP Server ────┐                    │\n│  │  mcp-server.ts              │ ← registerTool()   │\n│  │  (Streamable HTTP skeleton) │   from Phase 3     │\n│  └─────────────┬───────────────┘                    │\n│                │                                    │\n│  ┌──── Layer 4: Scheduler ─────┐                    │\n│  │  scheduler.ts               │                    │\n│  │  per-notebook FIFO queue    │──→ notifier.ts     │\n│  └─────────────┬───────────────┘   (best-effort)    │\n│                │ runTask()                          │\n│  ┌──── Layer 3: Agent Runtime ─┐                    │\n│  │  client.ts (singleton)      │                    │\n│  │  session-runner.ts          │                    │\n│  │  hooks.ts (4 lifecycle)     │                    │\n│  │  tools/ (8 tools)           │                    │\n│  └─────────────┬───────────────┘                    │\n│                │ CDP commands                       │\n│  ┌──── Layer 2: Infrastructure ┐                    │\n│  │  tab-manager.ts (Chrome)    │                    │\n│  │  network-gate.ts (backoff)  │                    │\n│  └─────────────┬───────────────┘                    │\n│                │                                    │\n│  ┌──── Layer 1: State ─────────┐                    │\n│  │  state-manager.ts           │                    │\n│  │  task-store.ts              │                    │\n│  │  cache-manager.ts           │                    │\n│  └─────────────────────────────┘                    │\n│                                                     │\n│  ┌──── Layer 0: Shared ────────┐                    │\n│  │  types.ts  errors.ts        │                    │\n│  │  config.ts logger.ts        │                    │\n│  └─────────────────────────────┘                    │\n└─────────────────────────────────────────────────────┘\n```\n\n## Phase 2 完成了什麼\n\n每一層都有 **獨立可測試的 skeleton**：\n- 227 個 unit tests 通過\n- TypeScript 編譯乾淨\n- 模組之間只靠 interface 溝通\n\n## Phase 2 還沒做什麼（Phase 3 邊界）\n\n1. **沒有 daemon entry point**：沒有人把 TabManager + CopilotClient + MCP Server + Scheduler 串起來\n2. **沒有 MCP tool 註冊**：mcp-server.ts 有 `registerTool()` 但沒有人呼叫\n3. **沒有 content pipeline**：`src/content/` 還是空的（repo/URL/PDF → text）\n4. **_handleUnexpectedExit 沒接線**：CopilotClient 的 auto-restart event 沒有綁定\n5. **result/error 沒 persist**：Scheduler executeTask 的 bug（見 Step 11）\n6. **多 MCP session 行為未驗證**：McpServer.connect() 多次呼叫的行為不明\n\n## 核心設計原則回顧\n\n- **Fail-open > Fail-closed**：NetworkGate、hooks 都是 fail-open\n- **Pull > Push**：get_status 是可靠通道，notification 是 best-effort\n- **Tool 自包**：visual tools 自動回傳 screenshot\n- **Atomic write**：所有持久化都用 temp + rename\n- **Singleton scope**：CopilotClient 和 NetworkGate 是 daemon 層級 singleton"
    }
  ]
}