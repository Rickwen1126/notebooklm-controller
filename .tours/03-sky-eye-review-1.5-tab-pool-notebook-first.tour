{
  "$schema": "https://aka.ms/codetour-schema",
  "title": "03 - Sky Eye: Review 1.5 Tab Pool + Notebook-First Architecture Boundaries",
  "steps": [
    {
      "title": "Context",
      "description": "## 這條 tour 在看什麼\n\n這條 tour 是在看：**NotebookLM Controller 從「使用者顯式管理 tab」轉向「Notebook-First + Tab Pool」後，每個模組的邊界變成什麼樣子**。\n\n如果先記不住細節，先抓住每個 step 只是在回答「這個模組的責任邊界在哪裡？它不管什麼？」\n\n## 這個系統是什麼\n\nNotebookLM Controller 是一個 MCP Server，透過 puppeteer-core 控制 Chrome 瀏覽器自動化操作 Google NotebookLM。使用者只說「幫我查這個 notebook」，系統自動取得瀏覽器 tab 資源、導航到正確頁面、執行操作、歸還資源。\n\n## 錨定已知\n\n如果你用過 database connection pool（如 pg-pool），Tab Pool 就是同一個概念：\n- Connection = Chrome Tab\n- acquire() = 從 pool 取一個 connection\n- release() = 歸還，不是 close\n- Pool full = 排隊等，不是報錯\n\n差別在：Tab Pool 有「弱親和」— 優先重用上次操作同一個 notebook 的 tab（省一次 navigate）。\n\n## Review Point 1.5 解決什麼問題\n\n之前架構有 `open_notebook` / `close_notebook` MCP tools，使用者要手動管理 tab 生命週期。這造成：\n1. 使用者需要理解「tab」這個內部概念\n2. 忘記 close 會洩漏資源，忘記 open 會報錯\n3. `NotebookEntry.active` 和 tab 開關混在一起\n\nReview 1.5 的核心決策：**Notebook 是產品概念，Tab 是內部資源。使用者不感知 tab。**\n\n## 走完後能建立什麼 mental model\n\n- 「誰擁有資源」的邊界：TabManager 擁有 tab 生命週期，Daemon 只做 acquire/release\n- 「什麼是產品概念 vs 內部概念」：Notebook = 使用者看到的，Tab = 系統內部資源池\n- 「Hint vs Assertion」：系統層做 O(1) 判斷，結果只是提示，agent 保留自主驗證權\n\n## 為什麼值得讀\n\n這是一個從「暴露內部概念給使用者」到「封裝內部資源管理」的重構案例。同樣的 pattern 出現在：DB connection pool、thread pool、K8s Pod scheduling。學會這個 pattern，你就能辨識其他系統中類似的邊界問題。"
    },
    {
      "file": "src/shared/types.ts",
      "line": 11,
      "ref": "25a58f582f54db5db73272da27b85881e3c1183b",
      "title": "Schema 邊界：NotebookEntry 不再有 active",
      "description": "## Insight\n\n**機制**：`NotebookEntry` 是使用者的產品概念（已註冊的 notebook）。移除 `active: boolean` 和 `NotebookStatus.\"closed\"` 後，notebook 的「是否有 tab」這件事從 schema 中消失了。註冊 = 存在，移除 = 消失，沒有中間態。\n\n**故障模式**：如果保留 `active` 欄位，系統會有兩個 source of truth（`active` flag 和 tab pool 中是否有 tab）。一旦不同步，使用者看到「active = true」但沒有 tab，或反過來。\n\n**設計取捨**：\n- 選擇：schema 不追蹤 tab 狀態 → tab 狀態完全由 TabManager runtime 管理\n- 代價：查詢「哪些 notebook 目前有 tab」需要去問 TabManager，不能光看 state.json\n- 收益：single source of truth，不會不同步\n\n---\n\n白話：NotebookEntry 現在就是一個「已註冊的 notebook」，不管它有沒有 tab。想知道有沒有 tab？去問 TabManager。這避免了「state.json 說 active 但 Chrome 裡根本沒開 tab」的鬼打牆。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：定義 `NotebookEntry` interface — 持久化在 `~/.nbctl/state.json` 的 notebook 資料。\n\n\n\n**為什麼這樣寫**：\n- 好處：schema 只反映「使用者決策」（註冊/移除），不反映「系統內部狀態」（有沒有 tab）\n- 好處：減少 MCP tool 數量（砍 open/close = 減少 2 個 API surface）\n- 壞處：如果未來需要離線查詢「哪些 notebook 上次有 tab」，state.json 裡沒有這個資訊\n\n**思考問題**：\n1. 如果系統重啟，所有 tab 都沒了。之前的 `active` flag 能幫什麼忙嗎？（答：幫不了，因為 tab 是 runtime 資源）\n2. `NotebookStatus` 只剩 `ready | operating | stale | error`，什麼情境會觸發 `stale`？\n\n## review\nURL stale 的正確語意是：已註冊 notebook 的 canonical URL 已失效，或 NotebookLM 明確回報該 notebook 不存在。這是 registry health 問題，不是 tab 狀態問題。型別與資料模型已經是這樣定義，見 types.ts:4 與 data-model.md:64。\n\nTab 是否剛好停在目標 notebook 頁面，是另一個完全不同的檢查。它只代表目前執行上下文對不對；不對就重新導向或重開 tab，不應直接推論 notebook 不存在。這點和 stale 必須分開。\n\n因此 preflight 可以放在同一個時機做兩件事，但兩者語意不可混用：\n\n檢查 tab context 是否對齊目標 notebook 頁面\n檢查註冊的 canonical URL 是否仍有效\n前者失敗是執行上下文修正，後者失敗才是 stale。\nerror 仍應保留給連線與執行層問題，例如 auth 過期、Chrome 斷線、timeout、network failure。也就是說：打不開不等於 stale；只有「這個 URL 指向的 notebook 已失效」才是 stale。規格目前也是這樣區分，見 spec.md:1161。\n\nactiveNotebooks\n\n目前 spec/data model 把 activeNotebooks 定義成「正在執行操作的 notebook alias 列表」，見 data-model.md:418。\n\n但目前實作不是這個意思；它直接回傳所有已註冊 notebook 的 key，見 mcp-tools.ts:141。所以現況下 activeNotebooks 其實比較接近 registeredNotebooks，不代表：\n\n目前有 tab\n目前正在跑 task\n目前 URL 可用\ncodebase 其實已經有更接近正確語意的 runtime source of truth：\n\nTab 使用狀態在 tab-manager.ts:231\nScheduler 正在處理哪些 notebook，在 scheduler.ts:47\n所以這裡的洞察可以定稿成一句話：\nNotebookEntry 只負責「已註冊 notebook」的持久化語意；tab lifecycle 與執行中狀態屬於 runtime；URL stale 屬於 registry health。現在最大的語意殘留是 activeNotebooks 命名與實作不一致。"
    },
    {
      "file": "src/shared/types.ts",
      "line": 152,
      "ref": "25a58f582f54db5db73272da27b85881e3c1183b",
      "title": "資源邊界：TabHandle 有了 pool state",
      "description": "## Insight\n\n**機制**：`TabHandle` 加了 `state: \"active\" | \"idle\"` 和 `releasedAt`。這是 resource pool 的核心概念 — 資源有「被佔用」和「可重用」兩種狀態。`releasedAt` 是 observability 欄位，用於追蹤 tab 在 idle 狀態停留多久。\n\n**故障模式**：如果沒有 `state` 欄位，`listTabs()` 無法區分「正在用」和「可以借」。`acquireTab()` 就沒辦法做弱親和，只能每次都開新 tab。\n\n**設計取捨**：\n- 選擇：在 `TabHandle` 上加 mutable state（`state` 可以從 active → idle → active）\n- 代價：`TabHandle` 不再是 immutable snapshot，是有狀態的 runtime object\n- 收益：`acquireTab` / `releaseTab` 可以直接 mutate 現有 handle，不需要 create/destroy\n\n---\n\n白話：Tab 以前只有「開」和「關」。現在多了「idle」— 像飯店退房但房間不打掃，下一個客人如果剛好住同一間就省了 check-in 時間。`state` 欄位就是記這個房態的。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：定義 `TabHandle` — runtime-only 的 Chrome tab 參考，包含 page reference 和 CDP session。\n\n**為什麼這樣寫**：\n- 好處：mutable state 讓 acquire/release 是 O(1) 操作（改欄位就好）\n- 好處：`releasedAt` 提供 idle duration 指標，未來可用於 idle tab 回收策略\n- 壞處：mutable object 在多處被引用，如果有並發修改可能有 race condition\n\n**思考問題**：\n1. `tab.url` 在 affinity reuse 時不會更新（不導航），這和 `tab.page.url()` 可能不一致。這會影響什麼？\n2. 如果未來要做 idle tab GC（回收閒置太久的 tab），`releasedAt` 怎麼用？\n\n## review \n問題一的真正描述：\nTabHandle.url 的現有語義其實沒有問題。它代表 tab 被建立或被重新指派時所屬的 notebook URL，用來表達這個 tab 服務哪個 notebook，而不是用來保證 tab 當前停留在哪個頁面。acquireTab() 的 affinity reuse 也刻意不導航，這是 pool 的設計選擇，不是 bug。真正的問題發生在後段：executor 的 start-page 判斷把 TabHandle.url 當成當前頁面依據，導致系統可能錯估 tab 是否已在 homepage 或 notebook page。\n解法：\n不要修改 TabHandle 的核心行為。要修的是 executor 啟動前的頁面校正流程。凡是要判斷「目前 tab 在哪一頁」或「是否需要先重導向」時，都必須讀取真實 live page 狀態，例如 tabHandle.page.url()，而不是依賴 TabHandle.url。如果 agent 的 startPage 與真實頁面不符，系統應在執行前提供正確 hint，或直接做顯式導航。\n影響：\n這樣修正後，tab pool 仍然保有 weak affinity 的效能優勢，同 notebook 任務可以延續前一個 tab 的現場；同時 executor 的起始頁面判斷也會回到正確語義，不再把 pool metadata 誤當成 live browser state。問題不在 tab handle 本身，而在「需要幫 tab 重定向時，判斷依據拿錯了」。\nTabHandle.url 是 assignment metadata，不是 current page fact。\nbug 不在 affinity reuse，而在後段 redirect / start-page 判斷把 metadata 當成 live state。\n\n\n問題二：未來若加入 idle tab GC，單靠 releasedAt + TTL 不足以保證正確性，必須由 pool-level 同步機制統一管理。\nreleasedAt 很適合用來表示 tab 進入 idle 的時間，並支援「閒置超過 TTL 就可回收」這種策略；但 TTL 只解決「該不該回收」，不解決「回收時會不會和 acquire 競爭同一個 tab」。如果 GC 在判斷某 tab 過期後直接 async close，而同時另一個 task 又 acquire 到這個 tab，就可能出現 tab 剛被借出又被關掉的 race condition。\n建議的設計是採用 pool-level mutex，讓 acquireTab、releaseTab、closeTab、未來的 sweepIdleTabs / evictExpiredTabs 都走同一套同步規則。也就是說，pool 是唯一有權改變 tab lifecycle 的地方，所有參與者都是一等公民，受同一套法律制約。releasedAt 只負責 TTL eligibility，真正的狀態轉移與所有權判定由 pool 在 lock 內原子完成。必要時可以增加內部 evicting 狀態，避免 tab 被 GC 選中後又重新借出。\n影響是：這會讓 tab pool 的生命週期模型一致、可推理、可擴充。未來加入 idle GC、容量控制、優先回收策略時，不需要把併發保護散落在各處，而是由 pool 統一負責，設計上更乾淨，也比較不會出現隱性 race。"
    },
    {
      "file": "src/tab-manager/tab-manager.ts",
      "line": 139,
      "ref": "25a58f582f54db5db73272da27b85881e3c1183b",
      "title": "Pool 邊界：acquireTab 三級優先策略",
      "description": "## Insight\n\n**機制**：`acquireTab()` 用三級 fallback 策略分配 tab 資源：\n1. **Affinity match**：找同一個 notebook 的 idle tab（不導航，最快）\n2. **Any idle**：找任意 idle tab，導航到目標 URL（重用 tab，省開新頁成本）\n3. **New tab**：開新 tab（最貴，受 MAX_TABS 限制）\n\nPool 滿且無 idle tab → 拋 `TabLimitError`（上游 Scheduler 負責排隊重試）。\n\n**故障模式**：如果只有策略 3（每次都開新 tab），10 個並發任務就會撞到 MAX_TABS 上限。有了 1+2，一個 tab 可以服務多個連續任務，提高資源利用率。\n\n**設計取捨**：\n- 選擇：策略 1 的 affinity match 不做導航\n- 代價：tab 的 DOM state 可能是「上一個任務結束時的狀態」，agent 需要自己判斷\n- 收益：避免不必要的 navigate（如果下一個任務操作同一個 notebook，省 3-5 秒頁面載入）\n\n---\n\n白話：它其實就是在做「借房間」。先看有沒有上次住過的空房（省打掃），再看有沒有任何空房（需要打掃），都沒有就開新房。全滿了就排隊。這三級策略讓 10 個 tab 能高效服務 100 個任務。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：Tab pool 的 acquire 邏輯，是 `TabManager` 最核心的新增方法。\n\n**為什麼這樣寫**：\n- 好處：O(n) 掃描（n = tab 數量 ≤ 10），實務上等同 O(1)\n- 好處：策略 2 的 `page.goto()` 只在真正需要時才觸發\n- 壞處：策略 1 的 affinity 是「弱」的 — 只看 `notebookAlias` 不看 URL exact match，如果 notebook URL 變了但 alias 不變，可能 reuse 了錯誤的 tab\n\n**思考問題**：\n1. 策略 2 做了 `tab.page.goto(params.url)`，如果 navigate 失敗（網路斷線），tab 處於什麼狀態？\n2. 為什麼不把 idle tab GC（超時自動關閉）放在 `acquireTab` 裡？\n\n## review\n第一題引出的問題\n這個問題的本質是 async shared-state race，不是多執行緒 race。雖然 Node.js 是 single-thread，但不同 notebook task 會並發進入 tab pool，而 acquireTab() 同時包含同步狀態修改與可能很慢的 await page.goto()。因此 pool 必須有互斥保護，否則會出現 double-acquire 或半完成狀態；但互斥也不能粗暴地包住整段 await，不然整個 pool 會被單一慢導航拖成全域串行。較合理的做法是：在短 critical section 內選 tab 並標記為 reserved/acquiring，離開 lock 後執行 goto()，成功後再 commit 為 active，失敗則 rollback 為 idle。這樣既保留 pool correctness，也不會把整個 pool 吞吐量鎖死\n\n第二題\n目前沒有 idle GC；releasedAt 和 timeoutAt 只是為未來回收策略或觀測預留的欄位，尚未實際驅動 tab 回收。\n\n"
    },
    {
      "file": "src/daemon/index.ts",
      "line": 64,
      "ref": "25a58f582f54db5db73272da27b85881e3c1183b",
      "title": "生命週期邊界：createRunTask 的 acquire-try-finally-release",
      "description": "## Insight\n\n**機制**：`createRunTask` 是 Scheduler 和 Tab Pool 的膠水。它做三件事：\n1. 從 `StateManager` 查 notebook URL（產品概念 → 資源 URL）\n2. 從 `TabManager.acquireTab()` 取 tab 資源（pool → handle）\n3. `try { runDualSession } finally { releaseTab }` 保證歸還\n\n這是典型的 **resource acquisition is initialization (RAII)** 模式的 async 版本。\n\n**故障模式**：如果沒有 `finally { releaseTab }`，任務失敗（exception）時 tab 不會歸還，pool 會慢慢耗盡。這是 connection pool leak 的經典 bug。\n\n**設計取捨**：\n- 選擇：`createRunTask` 擁有 acquire/release 的責任，不是 `runDualSession`\n- 代價：`runDualSession` 不知道 tab 的存在，它只看到 `notebookAlias` 和 `tabUrl` 字串\n- 收益：session runner 是純邏輯（prompt 組裝 + SDK 呼叫），不碰 resource management\n\n---\n\n白話：它其實就是在做「借房 → 辦事 → 還房」。不管辦事成不成功，最後一定還房間（`finally`）。`runDualSession` 只知道自己在哪個 notebook 操作，不知道 tab 怎麼來的。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：工廠函式，產生 Scheduler 每次跑任務時呼叫的 `runTask` callback。\n\n**為什麼這樣寫**：\n- 好處：清楚的責任分層 — Scheduler 管排程、createRunTask 管資源、runDualSession 管 AI session\n- 好處：`try/finally` 保證 resource leak safety\n- 壞處：`stateManager.getNotebook()` 每次任務都查一次磁碟，理論上可以快取\n\n**思考問題**：\n1. 如果 `acquireTab` 拋 `TabLimitError`，Scheduler 會怎麼處理這個任務？（答：目前返回失敗，未來應排隊重試）\n2. `tabHandle.url` 在傳給 `runDualSession` 時是 acquire 瞬間的值。如果 agent 在 session 中 navigate 了，這個值就過期了。這影響什麼？\n\n## review 補充\n\n這裡可以再多看一層：`finally { releaseTab }` 解決的是 leak safety，也就是「借了會還」；但 spec 還有另一個要求，是 pool 滿且全 active 時要等待 tab 空出，不把容量壓力翻譯成 task failed。\n\n所以第一個問題的關鍵，不只是「未來應排隊重試」，而是三個邊界的語意要不要對齊：Tab pool 的資源不足、Scheduler 的排隊語意、以及 task state machine 的 `queued/running/failed` 代表什麼。現在的實作是把「暫時借不到 tab」轉成 terminal failure，這和 producer-consumer 的脈絡其實不一致。\n\n第二個問題這裡只要先記得一句話就好：`tabHandle.url` 比較像 acquire 當下帶進來的 metadata，不是 live page fact。這個點會在同一條 tour 後面的 Pool 邊界與 Agent 邊界 step 再展開。"
    },
    {
      "file": "src/daemon/notebook-tools.ts",
      "line": 85,
      "ref": "25a58f582f54db5db73272da27b85881e3c1183b",
      "title": "API 邊界：砍掉 open/close = YAGNI 實踐",
      "description": "## Insight\n\n**機制**：`registerNotebookTools` 從 8 個 tools 砍到 6 個，移除了 `open_notebook` 和 `close_notebook`。使用者不再需要（也無法）手動控制 tab 生命週期。Tab 的 acquire/release 完全是系統內部事務。\n\n**故障模式**：如果保留 `open_notebook`，使用者可能手動 open 了一個 tab 但忘記 close，系統的 pool 會認為這個 tab 是「active」（被佔用），實際上沒有任務在用它。這是 resource leak 的根源。\n\n**設計取捨**：\n- 選擇：完全移除 open/close API（不是「保留但標 deprecated」）\n- 代價：如果使用者有「我想手動打開一個 notebook 來看看」的需求，沒有直接的方式\n- 收益：使用者不可能造成 resource leak，pool 狀態永遠一致\n\n---\n\n白話：以前使用者要自己「開 tab → 操作 → 關 tab」，像自己去停車場取車。現在變成 valet parking — 你只說要去哪，系統自動安排車位。使用者不需要知道「tab」這個東西。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：在 MCP server 上註冊所有 notebook 管理 tools。\n\n**為什麼這樣寫**：\n- 好處：API surface 變小 = 使用者犯錯的機會變少\n- 好處：減少了 `active: boolean` 的雙重 source of truth 問題\n- 壞處：如果 agent SDK 需要「預熱」某個 notebook 的 tab（提前 navigate 到頁面），目前沒有直接機制\n\n**思考問題**：\n1. 剩下的 6 個 tools 中，哪些會觸發 tab acquire？（答：只有 `exec` 相關的 tool 會，notebook 管理 tools 只操作 state.json）\n2. `add_notebook` 不再設 `active: true`。那新增一個 notebook 後，什麼時候才會「有 tab」？"
    },
    {
      "file": "src/daemon/mcp-tools.ts",
      "line": 137,
      "ref": "25a58f582f54db5db73272da27b85881e3c1183b",
      "title": "觀測邊界：get_status 的 tabPool 語義",
      "description": "## Insight\n\n**機制**：`get_status` 從 `tabManager: { activeTabs, maxTabs }` 改為 `tabPool: { usedSlots, maxSlots, idleSlots }`。關鍵變化：\n- 「activeTabs」混淆了「所有 tab」和「正在用的 tab」\n- 新語義明確區分：`usedSlots`（被任務佔用）、`idleSlots`（閒置可重用）、`maxSlots`（上限）\n- `activeNotebooks` 改為返回所有已註冊 notebooks（不再 filter `active: true`）\n\n**故障模式**：如果 `usedSlots + idleSlots > maxSlots`，說明有 bug（tab 被創建但沒進 pool）。這三個數字互相校驗，提供 observability。\n\n**設計取捨**：\n- 選擇：用 `tab.state` filter 即時計算，不用額外計數器\n- 代價：每次 `get_status` 都掃一遍所有 tab（O(n), n ≤ 10）\n- 收益：不需要維護 counter 的同步問題，值永遠正確\n\n---\n\n白話：以前只看得到「有幾個 tab 開著」。現在看得到「幾個在忙、幾個在待命、最多能開幾個」。就像飯店大廳看板：已入住 / 空房 / 總房數。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：建構 `DaemonStatusResult` 回應，MCP client 用來監控系統健康。\n\n**為什麼這樣寫**：\n- 好處：三個數字提供完整的 pool utilization 視圖\n- 好處：即時計算不會有 stale counter 問題\n- 壞處：`runningTasks` 還是 hardcoded 0（TODO），和 `usedSlots` 語義重疊\n\n**思考問題**：\n1. `usedSlots` 和 `runningTasks` 的語義差在哪裡？（答：一個 tab 可能服務多個連續步驟，`usedSlots` 是 tab 粒度，`runningTasks` 是任務粒度）\n2. 如果有 10 個 idle tab 和 0 個 used tab，系統效能有什麼問題？\n\n## review\n目前 MVP 裡，`usedSlots` 和 `runningTasks` 在實作上幾乎是 1:1。原因不是兩者本質相同，而是目前執行模型剛好把一個 running task 綁成必須獨占一個 tab，所以 task 顆粒與 tab 顆粒暫時重合。\n\n但語意上還是應該分開看：`usedSlots` 是 tab pool occupancy，`runningTasks` 是 scheduler task state。spec 把兩者分開列出是有意義的，因為這只是 MVP 的 implementation choice，不應被誤讀成長期架構保證。未來如果出現不需要整段 task 都綁住 tab 的執行模型，兩者就會自然分離。\n\n第二題目前可以很務實地定調成：如果系統有 10 個 idle tab 和 0 個 used tab，主要成本是 Chrome 記憶體占用，而不是 correctness 問題。這代表系統是在用記憶體換取後續 reuse 時少一次開 tab 或 navigate 的延遲。\n\nMVP 先不處理 idle GC 是合理的。只要 `MAX_TABS` 小、實際記憶體壓力可接受，就沒有必要提早引入回收機制與同步複雜度。等後面真的觀察到 idle tab 常駐造成資源壓力，再讓 `releasedAt` 接上 TTL/GC policy 即可。"
    },
    {
      "file": "src/agent/session-runner.ts",
      "line": 371,
      "ref": "25a58f582f54db5db73272da27b85881e3c1183b",
      "title": "Agent 邊界：Canonical Context + Pre-Navigate Hint",
      "description": "## Insight\n\n**機制**：Executor 的 systemMessage 現在注入兩層 context：\n1. **Canonical notebook context**（L372）：明確告訴 agent「你在操作哪個 notebook」— 避免跨 notebook 操作\n2. **Pre-navigate hint**（L374-387）：系統做 O(1) URL exact match，結果作為 hint 注入 prompt。Agent 可以信任 hint 或自己驗證\n\n**故障模式**：\n- 沒有 canonical context → agent 可能操作到錯誤的 notebook（如果 tab 上次停在別的頁面）\n- 如果 hint 是 assertion（「你已經在正確頁面」）→ agent 盲信，但 tab 實際可能已被導航到別處\n\n**設計取捨**：\n- 選擇：Hint, not assertion（系統提供線索，agent 保留驗證權）\n- 代價：agent 可能多做一次 `screenshot()` 來確認頁面狀態（多一次 tool call）\n- 收益：不會因為 hint 錯誤導致 agent 在錯誤頁面盲操作\n\n---\n\n白話：系統說「我看了一下，你應該在 notebook 頁面」，但 agent 可以自己看一眼確認。就像 GPS 說「你已到達目的地」，但你可以抬頭看門牌確認。這比 GPS 說「不用看了，一定是這裡」要安全。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：組裝 Executor session 的 systemMessage，依序是：tool constraint → notebook context → navigate hint → agent prompt。\n\n**為什麼這樣寫**：\n- 好處：hint 邏輯是 O(1) 字串比較，不做 DOM 查詢或截圖\n- 好處：GPT-4.1 是非推理模型，explicit context 比 implicit inference 可靠\n- 壞處：`HOMEPAGE_URL` 硬寫在函式裡，和 `notebook-tools.ts` 的 `NOTEBOOK_URL_PREFIX` 重複\n\n**思考問題**：\n1. `agentConfig.startPage` 是 `\"homepage\" | \"notebook\"`。什麼 agent 會在 homepage 操作？（答：`manage-notebook` 和 `sync-notebook`）\n2. 如果 tab 的 `page.url()` 是 `about:blank`（剛開的新 tab），pre-navigate hint 會說什麼？\n\n## review\n目前這一步有四個共識可以定稿。\n\n第一，pre-navigate 判斷是必修，而且目前這裡確實有 bug。問題不在 `startPage` 的概念，也不在 hint 模式本身，而在判斷依據拿錯了。現在 executor 用來做頁面錨定的是 `tabHandle.url`，但那是 tab assignment metadata，不是 live browser state。真正需要對齊的是 `tabHandle.page.url()` 這類即時頁面資訊，否則系統可能把「曾經被指派到哪個 notebook」誤判成「目前停在哪一頁」。\n\n第二，Planner 目前在編排 step 時，沒有拿到 canonical notebook context。也就是說，Session 1 現在知道 agent catalog、routing rules、locale，但不知道 daemon 已經 resolve 好的 target notebook 是哪一個。這會讓 Planner 只能從使用者自然語言猜 notebook，而不是以系統已決定的 notebook alias 為準。這個問題和 prompt 注入層次是連動的。\n\n第三，prompt 組合應升級成真正的 SDK `systemMessage`，而不是把 system prompt 當成 prompt prefix 手動拼接。比較乾淨的做法是：Planner 的 agent catalog、routing rules、canonical notebook context 放在 Planner session 的 `systemMessage`；Executor 的 tool constraint、canonical notebook context、page anchor hint、agent prompt 放在 Executor session 的 `systemMessage`；真正送進 `sendAndWait` 的只保留使用者命令或 `step.executorPrompt`。這樣 session-level policy 和 step-level instruction 才會分層清楚。\n\n第四，pre-navigate 的設計方向維持既有共識：系統先做頁面校正。也就是說，這裡不是只給 hint 然後完全交給 agent 自己決定，而是 runtime 應在 Executor 啟動前先根據 live page 狀態判斷是否需要 move page，把 tab 帶到符合 `agentConfig.startPage` 的起始頁，再讓 agent 開始執行。換句話說，hint 可以保留作為透明觀測資訊，但 page correction 屬於程式端責任，不應完全下放給 agent。"
    }
  ]
}