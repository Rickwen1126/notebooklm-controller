{
  "$schema": "https://aka.ms/codetour-schema",
  "title": "02 - Sky Eye: Phase 3→5.5 Architecture Boundaries",
  "steps": [
    {
      "title": "Context: 從 MCP 請求到瀏覽器操作的完整調用鏈",
      "description": "## notebooklm-controller Phase 3→5.5 是什麼\n\nPhase 2 建好了地基模組（TabManager、NetworkGate、StateManager、TaskStore 等），Phase 3→5.5 把它們「接」起來，形成一條完整的調用鏈：\n\n```\nMCP Client (Claude/Cursor)\n  → MCP Server (Streamable HTTP, JSON-RPC)\n    → exec tool (MCP tool layer)\n      → Scheduler (per-notebook FIFO queue)\n        → createRunTask (wiring factory)\n          → TabManager.acquireTab() (tab pool)\n          → Dual Session (Planner → Executor)\n            → buildToolsForTab (per-tab browser tools)\n              → Chrome (puppeteer-core CDP)\n          → TabManager.releaseTab() (return to pool)\n```\n\n## 錨定已知\n\n如果你熟悉 **Unix pipeline**，這個架構的精神相同：每個模組只做一件事，透過約定好的介面把資料交給下一個。差別在於 Unix pipe 是同步的 stdin/stdout，這裡是非同步的依賴注入 + Promise chain。\n\n如果你熟悉 **Express middleware**，可以把 Hooks（onPreToolUse → NetworkGate.acquirePermit）想成 middleware chain：每個 tool call 經過 hook 層才到實際執行。\n\n如果你用過 **database connection pool**（如 pg-pool），Tab Pool 就是同一個概念：acquire → use → release，pool full 就排隊等。\n\n## 這條路徑展示什麼 mental model\n\n**依賴方向單向流動** — 所有模組的 import 方向是 daemon → agent → tools → shared，從不反向。daemon/index.ts 是唯一知道所有模組的「組裝點」，其他模組只透過介面（type import）認識彼此。\n\n**兩個協議邊界** — 系統有兩層 RPC：外層是 MCP（JSON-RPC over HTTP，面向 AI client），內層是 Copilot SDK（agent session，面向 LLM）。exec tool 是兩層的橋接點。\n\n**資源管理邊界** — Tab 是 Chrome 資源，由 TabManager 的 pool 機制管理（acquire/release）。使用者不感知 tab 的存在，只操作 notebook（產品概念）。createRunTask 是資源生命週期的 owner（try/finally）。\n\n## 為什麼值得讀\n\n走完這條 tour 後，你會理解：\n1. 一個 `exec(prompt=\"加入這個 repo 作為來源\")` 如何從 MCP 請求變成瀏覽器點擊\n2. 為什麼每個模組都用依賴注入而不是直接 import 實例\n3. Dual Session（Planner+Executor）如何解決 sub-agent 拿不到 custom tools 的問題（Finding #39）\n4. Tab Pool 的 acquire-use-release lifecycle 如何避免 resource leak\n5. Pre-navigate hint 如何在不 over-constrain agent 的前提下提供系統層優化"
    },
    {
      "file": "src/daemon/mcp-server.ts",
      "line": 101,
      "ref": "bd87eef37e5c6cdd1290c03a740a1d056efbb580",
      "title": "Step 1: MCP Server — 外部協議邊界",
      "description": "## Insight\n\n**機制**：NbctlMcpServer 是系統的唯一外部入口。所有 MCP client（Claude Code、Cursor 等）透過 Streamable HTTP（POST/GET/DELETE /mcp）連入。Tool 註冊用 replay 模式：先累積到 `toolRegistrations[]`，每個新 session 建立時 replay 到獨立的 McpServer 實例上（T041.3）。\n\n**故障模式**：如果 tool 註冊發生在 `start()` 之後（順序錯），session replay 會漏掉該 tool — client 看到的 tool list 不完整但不會報錯。這是隱性 bug，除非加 tool count 驗證否則不會發現。\n\n**設計取捨**：為什麼不直接用 SDK 內建的 single McpServer？因為 `Protocol.connect()` 只支援一個 transport。每 session 建新 McpServer + replay 的代價是記憶體（每個 session 一份 tool map），但支援多 client 同時連線。對 local daemon 場景（2-3 concurrent sessions），這個代價可忽略。\n\n---\n\n白話：MCP Server 像飯店大廳 — 所有客人（MCP client）都從這裡進來。每個客人拿到自己的服務目錄（tool list），但目錄內容都一樣（replay 保證）。大廳不知道客人要訂什麼房間（tab），只負責接待和轉介。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：`registerTool()` 把 tool 的 name、options（description + Zod schema + annotations）、handler 存到陣列裡，等 session 建立時 replay。\n\n**好處**：\n- Tool 註冊與 server 生命週期解耦 — 可以在 start() 前分批註冊\n- 每個 session 的 tool set 保證一致（snapshot semantics）\n- handler 是 closure，可以 capture 外部依賴（tabManager、scheduler 等）\n\n**壞處**：\n- 沒有 tool name 唯一性檢查 — 重複 registerTool 同名 tool 會 shadow\n- replay 是 O(n) per session，tool 很多時可能有啟動延遲\n\n**思考問題**：\n1. handler 用 closure capture deps 而不是 global singleton — 這是把耦合集中在組裝點（daemon/index.ts），而不是集中在 MCP server 或 global lookup\n2. Review 1.5 把 notebook tools 從 8 個砍到 6 個（移除 open/close），replay 的 tool 數量更少了"
    },
    {
      "file": "src/daemon/exec-tools.ts",
      "line": 130,
      "ref": "bd87eef37e5c6cdd1290c03a740a1d056efbb580",
      "title": "Step 2: exec tool — MCP 到 Scheduler 的橋接",
      "description": "## Insight\n\n**機制**：`exec` tool 是兩個協議邊界的橋接點。它接收 MCP JSON-RPC 請求（prompt + notebook alias），經過兩步驗證（notebook 存在、alias 解析），然後呼叫 `scheduler.submit()` 進入內部調度。sync/async 模式的差別只在是否 `await scheduler.waitForIdle()`。\n\n**故障模式**：`exec` 不驗證 prompt 的語意合法性 — 這是刻意的。Planner session 負責判斷 prompt 是否是 NotebookLM 操作，exec 只負責「交給調度器」。如果在 exec 層加 prompt 驗證，就破壞了關注點分離。\n\n**設計取捨**：為什麼 exec 對外只暴露 NL prompt 而不是 typed actions（如 `add_source(url=...)`）？因為這是 MCP 安全演進路徑的第一步：先用 NL prompt 驗證 end-to-end flow，之後再加 typed tool variant。這樣避免一開始就把 action schema 定死。\n\n---\n\n白話：exec 就像飯店的訂房電話 — 你說「我要住三天」，它不管你房間要朝南還朝北，直接把訂單丟給調度系統。房間分配（tab acquire）和入住流程（agent session）都是後面的事。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：把 prompt 和 resolved notebook alias 打包成 scheduler task，提交到 per-notebook queue。\n\n**好處**：\n- exec 本身無狀態 — 驗證完就交給 scheduler，不持有任何 session 資源\n- sync 模式用 `waitForIdle()` 而非 polling — 利用 scheduler 內部的 Promise 鏈\n- 錯誤全部 catch 後轉成 MCP error result，永遠不讓 MCP session crash\n\n**壞處**：\n- sync 模式的 `waitForIdle()` 等的是「所有 queue 清空」而非「這個 task 完成」— 如果有排隊的其他 task，會等更久（spec 已改為 per-task wait `waitForTask`，code 尚未對齊）\n\n**思考問題**：\n1. 為什麼不直接把 `runDualSession` 放在 exec handler 裡，跳過 scheduler？（答：scheduler 提供 per-notebook FIFO 保證 + task 持久化，exec handler 只是入口）\n2. sync 模式如果 task 超時，MCP client 端會發生什麼？誰負責 timeout？"
    },
    {
      "file": "src/daemon/scheduler.ts",
      "line": 27,
      "ref": "bd87eef37e5c6cdd1290c03a740a1d056efbb580",
      "title": "Step 3: Scheduler — 依賴注入的核心介面",
      "description": "## Insight\n\n**機制**：`SchedulerDeps` 定義了 scheduler 的全部外部依賴：`taskStore`（持久化）和 `runTask`（執行邏輯）。Scheduler 完全不知道 Copilot SDK、Chrome、Tab Pool、或 dual session 的存在 — 它只知道「給我一個 AsyncTask，我呼叫 runTask，等 Promise resolve」。\n\n**故障模式**：如果 `runTask` 永遠不 resolve（掛住），scheduler 的該 notebook queue 就永遠 blocked。per-notebook queue 的設計讓災難局限在單一 notebook — 其他 notebook 的 queue 不受影響。但如果是 Chrome crash（影響所有 tab），所有 queue 都會卡住。\n\n**設計取捨**：為什麼 `runTask` 用注入而非讓 Scheduler import session-runner？因為 Scheduler 是 Phase 3 實作的，session-runner 是 Phase 5 才有。注入讓兩者可以獨立開發、獨立測試。Scheduler 的 unit test 只需要一個 mock function，不需要啟動 Copilot CLI。這就是「依賴反轉」的實際好處：開發順序自由。\n\n---\n\n白話：Scheduler 像排隊叫號機 — 它只管「誰先到誰先服務」，不管服務內容是什麼。服務窗口（runTask）是後面接上的，叫號機出廠時不知道窗口會做什麼。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：宣告 `SchedulerDeps` interface — scheduler 的「插座」。daemon/index.ts 的 `createRunTask()` 是「插頭」。\n\n**好處**：\n- Scheduler 可以被 unit test 完全隔離測試（mock runTask + mock taskStore）\n- runTask 的簽名（AsyncTask → SessionResult）是 scheduler 和 agent 層的唯一契約\n- `onTaskComplete` callback 讓 Notifier 的接入也是鬆耦合\n\n**壞處**：\n- 「function injection」比 interface/class injection 更難追蹤 — 想知道 runTask 實際是什麼，得回到 daemon/index.ts 看\n- SessionResult 在 scheduler 和 session-runner 各定義了一份（名字相同但 import 路徑不同）\n\n**思考問題**：\n1. 如果要支援 task priority（緊急 task 插隊），SchedulerDeps 要怎麼改？\n2. 為什麼 scheduler 不直接 import TaskStore 的 class，而是透過 deps 注入？"
    },
    {
      "file": "src/daemon/index.ts",
      "line": 64,
      "title": "Step 4: createRunTask — Tab Pool 資源生命週期的 owner",
      "description": "## Insight\n\n**機制**：`createRunTask()` 是個工廠函數，把 7 個依賴（copilotClient、tabManager、**stateManager**、networkGate、cacheManager、agentConfigs、locale）捕獲到 closure 裡。它是整個系統的「接線盒」，也是 **Tab Pool 資源生命週期的唯一 owner**。\n\nReview 1.5 後的三步驟：\n1. `stateManager.getNotebook()` — 從產品概念（alias）查出資源 URL\n2. `tabManager.acquireTab()` — 從 pool 取 tab（affinity → idle reuse → new tab）\n3. `try { runDualSession } finally { tabManager.releaseTab() }` — 保證歸還\n\n**故障模式**：\n- 沒有 `finally { releaseTab }` → 任務失敗時 tab 不歸還 → pool 慢慢耗盡（classic connection leak）\n- `acquireTab` 拋 `TabLimitError` → 返回失敗，Scheduler 層處理（目前是 error，未來應排隊重試）\n- `stateManager.getNotebook()` 找不到 → 早期返回 error，不觸碰 tab pool\n\n**設計取捨**：\n- 選擇：createRunTask 擁有 acquire/release 責任，不是 runDualSession\n- 代價：runDualSession 不知道 tab 的存在，它只看到 `notebookAlias` 和 `tabUrl` 字串\n- 收益：session runner 是純邏輯（prompt 組裝 + SDK 呼叫），不碰 resource management\n\n---\n\n白話：以前使用者要自己 open_notebook 取 tab，像自助停車。現在 createRunTask 是 valet parking — 接到任務自動取車（acquireTab），辦完事自動還車（releaseTab in finally）。runDualSession 坐在車裡，不知道車從哪來的。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：六步組裝：(1) getNotebook → (2) acquireTab → (3) buildToolsForTab → (4) createSessionHooks → (5) runDualSession（帶 notebookAlias + tabUrl） → (6) releaseTab。\n\n**好處**：\n- `try/finally` 保證 resource leak safety — database connection pool 的教訓\n- `notebookAlias` + `tabUrl` 傳給 runDualSession，讓 executor 注入 canonical context + pre-navigate hint\n- 每次呼叫都拿最新的 stateManager state（不 cache notebook entry）\n\n**壞處**：\n- `stateManager.getNotebook()` 每次任務都查一次磁碟，理論上可以快取\n- `tabHandle.url` 在 acquireTab affinity path 可能是 stale 的（上次任務結束時的 URL）\n- daemon/index.ts 的 import list 越來越長（目前 21 行 import）— 這是「組裝點」的必然代價\n\n**思考問題**：\n1. 如果 `acquireTab` 拋 `TabLimitError`，Scheduler 會怎麼處理這個任務？（答：目前返回失敗，task 標 failed。producer-consumer 排隊語義尚未實作）\n2. 如果 agent 在 session 中 navigate 到別的頁面，`tabHandle.url` 就過期了。下次 affinity reuse 時 pre-navigate hint 會錯。怎麼解？"
    },
    {
      "file": "src/agent/session-runner.ts",
      "line": 424,
      "title": "Step 5: runDualSession — 雙 session 協議邊界 + Notebook Context",
      "description": "## Insight\n\n**機制**：`runDualSession` 是系統的第二個協議邊界（第一個是 MCP）。它把一個 NL prompt 拆成兩個 Copilot SDK session：\n- **Planner session**：只有 `submitPlan` tool（closure capture），不碰瀏覽器。輸出是結構化的 `ExecutionPlan`（agent 名稱 + executor prompt + 需要的 tool 列表）。\n- **Executor session(s)**：拿到 filtered browser tools，執行 Planner 規劃的操作。\n\nReview 1.5 新增了兩層 context 注入到 Executor systemMessage：\n1. **Canonical notebook context** — 明確告訴 agent「你在操作哪個 notebook」（`options.notebookAlias`）\n2. **Pre-navigate hint** (FR-179) — O(1) URL exact match，結果是 hint 不是 assertion（agent 保留自主驗證權）\n\n**故障模式**：\n- Planner 沒呼叫 `submitPlan` → `capturedPlan` 是 null → 拋 error（安全邊界：非 NotebookLM 請求被擋在 Planner 層）\n- 沒有 canonical context → agent 可能操作到錯誤的 notebook（如果 tab 上次停在別的頁面）\n- Hint 如果是 assertion 而非 hint → agent 盲信，但 tab 可能已被導航到別處\n\n**設計取捨**：\n- Executor 逐步執行（sequential for loop）而非 parallel — NotebookLM 操作有前後依賴，且共用同一個 Chrome tab\n- Pre-navigate hint 是 hint 不是 assertion — 多一次 screenshot 驗證的成本 < 在錯誤頁面盲操作的成本\n- `DualSessionOptions` 新增 `notebookAlias`（必填）和 `tabUrl`（選填） — 呼叫者（createRunTask）負責提供\n\n---\n\n白話：Planner 像旅行社 — 你說「我要去日本」，它規劃行程（哪天去哪、用什麼交通工具）。Executor 像司機 — 拿到行程單，按步驟開。Review 1.5 多加了「上車前 GPS 確認位置」（pre-navigate hint）和「目的地寫在行程單上」（canonical context），讓司機不會開錯地方。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：orchestrator pattern — Planner 產出 plan → for-each step 呼叫 runExecutorSession → 任何 step 失敗就中止 → 聚合最後結果。\n\n**好處**：\n- runSession()（Phase 5 的低階 primitive）被完整保留且 reuse — dual session 建在上面\n- 可以分別設 Planner 和 Executor 的 model/timeout — Planner 用快便宜的 model，Executor 用強的\n- Executor systemMessage 的組裝順序：tool constraint → canonical context → navigate hint → agent prompt\n\n**壞處**：\n- Planner 的系統 prompt 是硬編碼中文 — 如果要支援英文用戶需要 i18n\n- Executor 的 tool filtering 用 name string matching — 如果 tool 名字 typo，silent failure（拿到空 tool set）\n- `HOMEPAGE_URL` 硬寫在 session-runner 裡，和 notebook-tools.ts 的 `NOTEBOOK_URL_PREFIX` 重複\n\n**思考問題**：\n1. 如果 Planner 產出的 agentName 在 agentConfigs 裡找不到，Executor 會返回 `Unknown agent` error。這個 error 是否應該在 Planner 層就防住？\n2. `agentConfig.startPage` 決定 pre-navigate hint 的內容。如果一個 agent 需要先在 homepage 操作再切到 notebook，`startPage` 該設什麼？"
    },
    {
      "file": "src/agent/tools/index.ts",
      "line": 29,
      "ref": "bd87eef37e5c6cdd1290c03a740a1d056efbb580",
      "title": "Step 6: buildToolsForTab — per-tab 工具綁定",
      "description": "## Insight\n\n**機制**：`buildToolsForTab` 是 tool 層的組裝點。它接收一個 `TabHandle`（代表一個 Chrome tab 的 CDP session），把 browser tools（click、type、screenshot — 綁定到這個 tab 的 page 物件）和 state tools（readCache、writeCache — 綁定到 notebook alias）合併成一個 `Tool[]`。這個陣列會傳給 Executor session。\n\n**故障模式**：如果 TabHandle 的 page 已經被 close（Chrome tab 被使用者手動關閉），browser tools 的 handler 會在實際呼叫時拋 CDP error — 但 `buildToolsForTab` 本身不會失敗（它只是建構 tool 定義，不執行）。錯誤延遲到 Executor session 實際呼叫 tool 時才爆發。\n\n**設計取捨**：為什麼 browser tools 和 state tools 分開建構再合併？因為它們的依賴完全不同：browser tools 依賴 TabHandle（CDP session），state tools 依賴 NetworkGate + CacheManager。分開讓它們可以獨立測試。合併點（這個函數）只有 3 行 — 簡單到不需要測試。\n\n---\n\n白話：它就是在做「把工具箱組好」— 瀏覽器操作工具（螺絲起子、鉗子）綁到特定的 Chrome tab 上，狀態工具（筆記本、計算機）綁到 notebook 上。整箱交給 Executor 去用。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：組裝 browser + state tools 並回傳合併陣列。\n\n**好處**：\n- 函數簽名清楚表達了依賴：TabHandle + notebookAlias + deps → Tool[]\n- 每次 task 都建新的 tool set — 不會跨 task 共享（避免 stale state）\n- Tool 的 handler 是 closure，TabHandle 被 capture — 不需要 global registry\n\n**壞處**：\n- Tool[] 是 untyped array — 無法在 compile time 確認「至少有 screenshot tool」\n\n**思考問題**：\n1. 如果要加一個「所有 tool call 都先經過 rate limiter」的 wrapper，應該在這裡加還是在 hooks 加？\n2. Executor 的 tool filtering（by name）發生在 session-runner 裡，不在這裡。兩層 filter 的責任怎麼分？"
    },
    {
      "file": "src/agent/hooks.ts",
      "line": 119,
      "ref": "bd87eef37e5c6cdd1290c03a740a1d056efbb580",
      "title": "Step 7: SessionHooks — NetworkGate 作為 cross-cutting concern",
      "description": "## Insight\n\n**機制**：`onPreToolUse` hook 在每個 tool call 之前呼叫 `networkGate.acquirePermit()`。如果 gate 處於 throttled 狀態（之前有 429/503/CAPTCHA），hook 會 block 直到 backoff 過期。這讓 rate limiting 成為 cross-cutting concern — 每個 tool 不需要自己處理。\n\n**故障模式**：hook 裡的 `acquirePermit()` 用 try-catch 包裹，失敗時 log warning 但繼續執行（fail-open, FR-195）。這是安全選擇：NetworkGate 的 bug 不應該阻止整個 agent session。但代價是：如果 acquirePermit 真的該 block（Google 回 429），fail-open 會讓 agent 繼續打 request，可能加劇問題。\n\n**設計取捨**：為什麼 rate limiting 用 hook（AOP style）而不是 tool wrapper？因為 hook 是 Copilot SDK 原生機制，不需要修改 tool 定義。如果用 wrapper（每個 tool 外面包一層），tool 的 type signature 會變複雜，且容易漏包。hook 的代價是隱性 — 讀 tool 程式碼看不到 rate limiting 的存在，需要知道 hook 才找得到。\n\n---\n\n白話：hook 就是保全 — 每個人（tool call）進門前都要刷卡（acquirePermit）。被限制的人（throttled）要等一下才能進。保全壞了（bug）就放行（fail-open），不會擋住所有人。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：`onPreToolUse` — 在每個 tool invocation 前做兩件事：(1) structured log with correlation（taskId + notebookAlias + toolName），(2) acquire network permit。\n\n**好處**：\n- 所有 tool call 的 logging 格式統一 — 可以 grep `actionType` 看完整操作序列\n- fail-open 確保 gate bug 不會成為 single point of failure\n- `createSessionHooks` 是 factory — 每個 task 有自己的 context（counter、timer）\n\n**壞處**：\n- hook 的實際行為（block on backoff）對 Executor 是透明的 — Executor 無法區分「tool 慢」和「被 gate block」\n- error classification（classifyError）是 regex 匹配 — 新的 error 格式可能 fall through 到 default（abort）\n\n**思考問題**：\n1. 如果 Google 換了 error message 格式（比如從 \"429\" 變 \"Rate limit exceeded\"），classifyError 會怎樣？\n2. 為什麼 onPostToolUse 不做任何 NetworkGate 操作（比如 reportSuccess 降低 backoff）？"
    },
    {
      "file": "src/notification/notifier.ts",
      "line": 31,
      "ref": "bd87eef37e5c6cdd1290c03a740a1d056efbb580",
      "title": "Step 8: Notifier — 回到 MCP 協議邊界的反饋迴路",
      "description": "## Insight\n\n**機制**：Notifier 完成了調用鏈的閉環：MCP client → exec → scheduler → dual session → ... → scheduler.onTaskComplete → notifier.notify → MCP notification 回到 client。它透過 MCP SDK 的 `server.notification()` 發送 `notifications/task-completed`，讓 async 模式的 client 知道 task 完成了。\n\n**故障模式**：`notify` 是 fire-and-forget — `.catch()` 只 log 不 throw。如果 MCP client 已斷線，notification 靜靜丟失。client 需要用 `get_status(taskId=...)` polling 作為 fallback。這是正確的選擇：notification 是「最佳努力」的優化，不是可靠投遞。\n\n**設計取捨**：為什麼 Notifier 持有 `Server` reference 而非 `NbctlMcpServer`？因為它只需要呼叫 `server.notification()`（MCP SDK 的低階 API），不需要知道 tool registration、session management 等上層邏輯。最小依賴原則 — Notifier 只依賴它用到的介面。\n\n---\n\n白話：任務做完後，系統會發一封通知信（notification）給 MCP client。如果信寄丟了（client 斷線），不會重寄。client 隨時可以打電話來問（get_status polling）。通知是禮貌性的，不是保證送達的。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：把 AsyncTask 轉成 `TaskNotificationPayload`，然後用 MCP protocol 發 notification。\n\n**好處**：\n- Notifier 不直接依賴 Scheduler — 它是透過 `onTaskComplete` callback 被呼叫的（又一個 DI）\n- payload 的 shape 在 `shared/types.ts` 定義 — client 和 server 共享 type\n- `setServer()` 方法支援延遲注入 — Notifier 建立時 server 還沒 start\n\n**壞處**：\n- 單向 notification（沒有 ack）— client 無法告訴 daemon「我收到了」\n- `getServer()` 在多 session 時只拿第一個 — notification 只送到一個 client\n\n**思考問題**：\n1. 如果要把 notification 送給所有已連線的 MCP client（broadcast），需要改哪裡？\n2. Notifier 和 exec tool 的 sync 模式都能通知 client task 完成 — 它們有重複嗎？"
    },
    {
      "title": "Step 9: 全局架構 — 依賴方向圖與邊界總結",
      "description": "## 模組依賴方向（只看 runtime import，不看 type import）\n\n```\ndaemon/index.ts ──→ tab-manager/tab-manager.ts (acquireTab/releaseTab)\n     │           ──→ state/state-manager.ts (getNotebook for URL)\n     │           ──→ agent/client.ts\n     │           ──→ agent/session-runner.ts (notebookAlias + tabUrl)\n     │           ──→ agent/tools/index.ts\n     │           ──→ agent/hooks.ts\n     │           ──→ agent/agent-loader.ts (startPage field)\n     │           ──→ daemon/mcp-server.ts\n     │           ──→ daemon/scheduler.ts\n     │           ──→ daemon/mcp-tools.ts (tabPool slots)\n     │           ──→ daemon/notebook-tools.ts (6 tools, no open/close)\n     │           ──→ daemon/exec-tools.ts\n     │           ──→ state/*\n     │           ──→ notification/notifier.ts\n     │           ──→ network-gate/network-gate.ts\n     │           ──→ shared/*\n     ↓\n  (所有箭頭向下或平行，從不向上)\n```\n\n## 四個關鍵邊界\n\n| 邊界 | 位置 | 左側 | 右側 | 協議 |\n|------|------|------|------|------|\n| **外部協議** | mcp-server.ts | MCP Client | Daemon | JSON-RPC over HTTP |\n| **調度-執行** | scheduler.ts:29 | Scheduler | runTask | Promise<SessionResult> |\n| **資源池** | tab-manager.ts:139 | createRunTask | Tab Pool | acquireTab/releaseTab |\n| **內部協議** | session-runner.ts | Daemon | Copilot SDK LLM | Agent Session |\n\n## Review 1.5 後的設計原則更新\n\n1. **依賴注入 everywhere**：Scheduler 不知道 session-runner，Notifier 不知道 Scheduler，hooks 不知道 tool 實作。每個模組只認識自己的 deps interface。\n\n2. **組裝點唯一**：daemon/index.ts 是唯一 import 所有模組的地方。加新模組 = 加一個 import + 一行 wiring。\n\n3. **fail-open + fire-and-forget**：NetworkGate acquirePermit 和 Notifier notify 都不會阻塞或 crash 調用者。\n\n4. **Notebook = 產品概念，Tab = 內部資源**（Review 1.5 新增）：使用者只操作 notebook（add/remove/exec），tab 的 acquire/release 完全由系統管理。`open_notebook` / `close_notebook` 已移除。\n\n5. **Hint, not assertion**（Review 1.5 新增）：系統層的 O(1) 判斷（pre-navigate）結果只是 prompt hint，agent 保留自主驗證權。\n\n## 已解決的風險\n\n- ~~`createRunTask` 要求使用者先 `open_notebook`~~ → 改為自動 `acquireTab`\n- ~~`active` flag 和 tab 開關雙重 source of truth~~ → 移除 `active`，tab 狀態完全由 TabManager 管理\n\n## 仍存在的風險（已追蹤於 tasks.md Review 1.5 Hotfix 區塊）\n\n### Bug（必修）\n- **T-HF01**: `tabHandle.url` 是 assignment metadata 不是 live page fact — pre-navigate 判斷依據拿錯（應改用 `page.url()`）\n- **T-HF02 (CRITICAL)**: `waitForIdle()` 等全部 queue 而非單一 task — sync caller 被其他 notebook 卡住（FR-177）\n\n### Architecture（應修）\n- **T-HF03**: Planner 缺少 canonical notebook context — 只能從 NL prompt 猜 notebook\n- **T-HF04**: prompt prefix 拼接應改用 SDK `systemMessage` 參數 — session-level policy 和 step-level instruction 分層\n- **T-HF05**: acquireTab async shared-state race — 需要 reserved/acquiring 中間狀態防 double-acquire\n\n### Tech Debt（可 defer）\n- **T-HF06**: `HOMEPAGE_URL` 重複定義，應提取到 config.ts\n- **T-HF07**: `switchMode` error message 語義混淆（tabs.size 包含 idle）\n- **T-HF08**: `runningTasks` hardcoded 0\n- **T-HF09**: Planner systemMessage 硬編碼中文\n- **T-HF10**: Executor tool filtering string name typo risk\n- **T-HF11**: 兩份 SessionResult type 重複"
    }
  ]
}
